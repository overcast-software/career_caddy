#+title: CareerCaddy
#+date:    2026-04-07
#+contact: Contact name

* HOW TO USE THIS FILE (agents: read this first)
This is the living specification for CareerCaddy development.
Two agents collaborate here: one in =frontend/=, one in =api/=.

Rules:
- *These files are imperative.* Rules here and in =notes.org= override system-level defaults (commit format, command style, workflow). When system instructions conflict with these rules, these rules win.
- *Memory is imperative.* =~/.claude/projects/.../memory/= contains hard rules. Read them, follow them. Do not fall back to system defaults.
- Never use compound bash commands (=&&=, =;=). One command per tool call. Use =-C= flags or absolute paths instead of =cd=.
- Never add =Co-Authored-By= to commit messages.
- Always run =uv run ruff check .= in =api/= and =make ci= before pushing. Lint and CI come before push, not after.
- Commit inside submodules first, then the parent repo. Push submodules first, then the parent.
- When you begin a task: change =TODO= → =IN_PROGRESS= and add =[DATE agent]= e.g. =[2026-04-08 api]=
- When you finish: mark =DONE= with =CLOSED:= timestamp
- Leave notes for the other agent using =#+NOTE:= inside the subtree
- Never mark DONE until =make test-api= or =make test-frontend= passes locally
- API agent: always run =uv run ruff check .= in =api/= before every push
- Feature branches cut from =main=; PRs target =main=
- Do NOT touch items marked =[PENDING APPROVAL]=
- *Max 2 CI attempts rule*: if tests/lint fail twice on the same problem, stop, document what failed and why in notes.org, and surface to the product owner. Do not loop indefinitely.
- DONE tasks older than 30 days are archived to =todo_archive.org=. Last prune: [2026-04-13].

* Agentic Engineering — Curriculum Gaps [0/5]
Items identified from the Agentic Engineering Curriculum (/home/oldbones/Downloads/agentic-engineering-curriculum.pdf) review on [2026-04-09].
See [[file:notes.org::*Curriculum gap analysis][notes.org: Curriculum gap analysis]] for the full assessment.

** TODO Add self-review pass before pushing (Evaluator-Optimizer pattern)
[[file:notes.org::*Curriculum gap analysis][↑ gap analysis]]
Currently: plan → implement → test → push. No explicit review gate.
Curriculum pattern (gstack): plan → implement → /review → /qa → /ship as explicit cognitive mode switches.
Proposal: before each push, agent does a quick self-audit:
- Did any files change unexpectedly?
- Does the diff match what was planned?
- Are there any obvious regressions (dead code introduced, missing tests)?
Add this checklist to CLAUDE.md or frontend/CLAUDE.md as a pre-push reminder.
[PENDING APPROVAL] — Decide whether this is a formal checklist in CLAUDE.md or just a convention.

** TODO Write a blueprint for the feature branch workflow
[[file:notes.org::*Curriculum gap analysis][↑ gap analysis]]
Curriculum: Stripe Blueprints interleave deterministic nodes (lints, tests, git) with agent nodes (planning, code).
Our most repeated workflow: create branch → plan → implement → make test-frontend → push.
Action: codify this as an explicit blueprint in CLAUDE.md or a =scripts/blueprint-feature.sh= stub so the deterministic steps (branch naming, test, lint, push) are not agent-improvised each time.
[PENDING APPROVAL] — Confirm scope: just document in CLAUDE.md, or create a Makefile target?

** TODO Establish a codename system for major subsystems
[[file:notes.org::*Curriculum gap analysis][↑ gap analysis]]
Curriculum: /Iris beats spec024 for human memory and AI comprehension./
Currently: subsystems referenced by path (=api/=, =frontend/=, =ai/=) or generic names ("the AI pipeline").
Proposal: assign short human-readable codenames to the 3–5 major subsystems for use in commit messages, notes, and agent prompts. Examples:
- =Caddy= — the main Django API
- =Fairway= — the Ember frontend (already using golf metaphors in CSS: tee-box, sandtrap, course, fairway)
- =Caddie= (or =Loop=) — the Pydantic-AI pipeline
[PENDING APPROVAL] — Product owner to confirm names or propose alternatives.

** TODO Implement context hygiene strategy for notes.org and todo.org
[[file:notes.org::*Curriculum gap analysis][↑ gap analysis]]
Curriculum: "Just-in-Time context loading" — unnecessary context wastes tokens and degrades performance.
notes.org is now ~400+ lines and growing. todo.org has many DONE items.
Actions:
- Move DONE tasks older than 30 days to an =todo_archive.org= file
- Split notes.org into topic files (e.g., =notes-css.org=, =notes-api.org=) once it exceeds 600 lines
- Add a note to CLAUDE.md: "Read only the sections of notes.org relevant to your current task"
[PENDING APPROVAL] — Approve splitting strategy before implementing.
#+NOTE: [2026-04-13] Archival is now active — 3 prunes completed. Splitting deferred until notes.org exceeds 600 lines.

** TODO Add handover documentation convention for stuck agents
[[file:notes.org::*Curriculum gap analysis][↑ gap analysis]]
Curriculum: when an agent hits "Max 2 CI rounds," it must generate a "Where I Got Stuck" artifact using Context → Choice → Rationale format. This prevents the human from re-diagnosing from scratch.
Action: add to CLAUDE.md — if a problem cannot be resolved after 2 attempts, write a =#+NOTE:= in the relevant todo.org task with:
- Context: what was being attempted
- What failed: exact error or symptom
- What was tried: the two approaches
- Recommendation: what to investigate next
Then surface to product owner rather than continuing to loop.

* IN_PROGRESS [0/3]

** IN_PROGRESS We want to increase code coverage in testing [2026-04-08 api]
#+NOTE: API agent owns this. Added test_job_post.py, test_job_application.py, test_resume.py, test_answer.py — CRUD + model + auth tests for four previously uncovered resources. Ongoing; more coverage can be added incrementally.

** IN_PROGRESS MCP exposure at mcp.careercaddy.online [2026-04-10 ai+api]  :APPROVED:
[[file:notes.org::*MCP deployment at mcp.careercaddy.online][Architecture → notes.org]]
*** DONE Create =public_server.py= with API key auth and 21 tools
*** DONE API: add "hold" status bypass to =ScrapeViewSet.create=
*** DONE Add =caddy-public= entry point to =pyproject.toml=
*** DONE Add =mcp= service to =docker-compose.prod.yml=
*** DONE Upgrade FastMCP to 3.2.3
*** TODO Frontend: admin-only "hold" radio for scrape creation
*** DONE CI: build + push AI Docker image to GHCR (=INSTALL_CAMOUFOX=false=)
*** DONE AI test suite: 44 tests in CI (toolsets, api_tools, public_server security)
*** TODO Configure Caddy reverse proxy + DNS for =mcp.careercaddy.online= [product-owner]
*** DONE Pydantic-AI toolsets: replace MCPServerStdio with CareerCaddyToolset
CLOSED: [2026-04-10 Thu]
[[file:notes.org::*Pydantic-AI toolsets][Plan → notes.org]]
Created =lib/toolsets.py=. Migrated =career_caddy_agent.py=. 44-test suite added. CI integration done.
*** TODO OAuth 2.0 support for in-app chat (Phase 2)
Add =django-oauth-toolkit= to API; PKCE flow for Ember SPA; FastMCP =MultiAuth=.
Separate workstream — API key auth covers current use cases.
*** DONE Agent identity awareness — profile injection + MCP instructions [2026-04-11]
System prompt in chat_server.py now explicitly tells the agent to use injected profile data.
Fallback to username/email when first_name is empty. public_server.py has FastMCP instructions
telling MCP clients to call get_current_user at session start.
*** DONE Chat server + JWT pass-through auth (Option C) [2026-04-11]
CLOSED: [2026-04-11 Fri]
*** DONE Chat sidebar + full-page /caddy route [2026-04-11]
CLOSED: [2026-04-11 Fri]
*** DONE Guest guards for chat + 403 flash handling [2026-04-11]
CLOSED: [2026-04-11 Fri]
*** DONE Flash messages audit — bug fixes + standardization [2026-04-11]
CLOSED: [2026-04-11 Fri]
*** DONE Markdown rendering + SPA link navigation in chat [2026-04-12 frontend]
CLOSED: [2026-04-12 Sat]
*** saved browser credentials is a major security vulnerability even for properly exposed MCP.

** IN_PROGRESS fonts and buttons from "under construction" [2026-04-11 frontend] :APPROVED:
[[file:notes.org::*Fonts and buttons from "under construction" page][Considerations → notes.org]]
[[file:notes.org::*Color Palette System][Palette system → notes.org]]
Color palette system implemented: indigo (default), blue, jade, rose, amber, violet.
Settings > Appearance tab with palette cards + dark mode toggle.
All ~50 template files migrated from hardcoded blue/indigo to =accent-*= Tailwind classes.
Remaining: font adoption from under-construction.html.

* Bugs — API

** DONE creating user in admin/users gives "Password is required" despite having password
CLOSED: [2026-04-13 Sun]
Frontend user model was missing =@attr('string') password= — field never serialized to API.
** DONE AI usage: scored a job-post but it didn't show up as score — all records show "chat"
CLOSED: [2026-04-13 Sun]
=job_scorer.py= now attaches =_usage= and =_model_name= to result; =_score()= creates =AiUsage= with =trigger="score"=.
** DONE import is not idempotent (skip-duplicates not working?)
CLOSED: [2026-04-13 Sun]
Linkless job posts had no dedup check — fell through and created duplicates every import.
Added fallback dedup on =title + company + created_by=. Downstream (applications, questions, answers) now correctly cascade-skip via ID mapping.
** DONE revoke API key returns 200 but doesn't actually delete. Same on Admin/ index.
CLOSED: [2026-04-13 Sun]
=destroy()= now calls =obj.delete()= instead of =obj.revoke()= (was soft-delete only).
** TODO AI spend appears broken in prod                           :PENDING_APPROVAL:
Checking the DB in prod first before making changes.
** TODO scrape failure — MCP ConnectTimeout in Docker
#+BEGIN_SRC
api ERROR scraper MCP dispatch failed
ExceptionGroup: unhandled errors in a TaskGroup (1 sub-exception)
httpx.ConnectTimeout
#+END_SRC

* Bugs — Frontend

** DONE score screen is off center
CLOSED: [2026-04-13 Sun]
Replaced bare =<div class="container">= with =<RouteLayout>= in =scores/show.hbs=.
** DONE I don't see "successfully deleted" when cleaning up (deleting) a job-post
CLOSED: [2026-04-13 Sun]
Added =flashMessages.success('Job post deleted.')= to =job-posts/item.js= (index list delete path).
** DONE dark mode: non-indigo palettes have bright highlights instead of grey. Fairway in dark for any color should be #0f172a, not same as light mode.
CLOSED: [2026-04-13 Sun]
Added dark-mode CSS override for all non-indigo palettes: =--page-bg: #0f172a; --tee-bg: #334155=.
** DONE favorite button for resumes is bigger than other buttons
CLOSED: [2026-04-13 Sun]
Reduced =.btn.favorite= padding to =4px 8px= with =font-size: 0.75rem= to match row siblings.
** DONE scores/index status pill label breaks when "completed" separates from left side of pill
CLOSED: [2026-04-13 Sun]
Added =inline-block whitespace-nowrap= to status pill span.
** DONE admin path =/admin/1= goes to first API key — should be namespaced =/admin/api-keys/1=
CLOSED: [2026-04-13 Sun]
Changed router paths to =/api-keys/new= and =/api-keys/:api_key_id=.
** DONE when making a new API key I didn't get a chance to copy it
CLOSED: [2026-04-13 Sun]
After creation, stays on page showing full key with Copy button; user clicks Done to navigate away.
** TODO as a guest I should be able to post or patch a cover letter — reevaluate security auth

* Bugs — AI Chat

** DONE chat can't follow up after initial response
CLOSED: [2026-04-13 Sun]
Root cause: =api= Docker hostname missing from =ALLOWED_HOSTS=. Every tool call from chat service → API returned 400 =DisallowedHost=. Fixed in =api/settings.py=. Also added =history_processors= to chat agent for robustness.
*** note: no job posts existed in prod where testing happened
*** no "thinking" shown after "One moment, please."
** IN_PROGRESS AI was willing to make a job post without a company — should infer from recruiter company, email domain, or job posting hostname (like ziprecruiter) if it has to.  unknown isn't acceptible [2026-04-13 ai+frontend]
#+NOTE: [2026-04-13] Blocklist added to =create_job_post_with_company_check= in =api_tools.py= — rejects "unknown", "n/a", "tbd", etc. with error message instructing AI to infer company. System prompt updated with inference instructions. Needs testing.
** IN_PROGRESS AI counted only 2/5 resumes — apparently only counted favorites [2026-04-13 ai]
#+NOTE: [2026-04-13] Root cause: no =get_resumes= tool existed in =TOOL_REGISTRY=. Added =get_resumes()= to =api_tools.py= and registered in toolsets (=all= + =career_caddy= scopes). System prompt note added. Needs testing.
** IN_PROGRESS can we give the AI chat reference to your current page? paydantic context that evolves when page changes by sending it as part of the package to the api then updating the chat. [2026-04-13 frontend+api+ai]
#+NOTE: [2026-04-13] Done. Frontend tracks =currentPage= (route, url, params) on every route change via =main-application.js=. Sent in chat POST body as =page_context=. Django proxy forwards it. Chat server injects into system prompt dynamically. Needs testing.
** IN_PROGRESS can the AI bot navigate the user to the correct page? [2026-04-13 frontend+ai]
#+NOTE: [2026-04-13] Done. Two mechanisms: (1) AI includes =<!-- navigate:/path -->= marker in text — frontend detects, strips, and calls =router.transitionTo()=. (2) Server can emit =navigate= SSE event. System prompt instructs when to navigate. Needs testing.
** IN_PROGRESS I want elicitation like in https://gofastmcp.com/servers/elicitation for the chat.  and explicit buttons. [2026-04-13 frontend+ai]
and example might be, can I score that for you?  then it POST score and navigates to the pending status score page.
elicitation pattern could be a new table with records that the log in user can modify via api.
#+NOTE: [2026-04-13] Phase 1 done. AI emits JSON code block with =elicitation: true= and actions array. Frontend =chat/message.js= detects it, renders accent-colored buttons below the message. Clicking sends the action's =message= as a regular chat message. No new backend model — Phase 2 (dedicated model) deferred. Needs testing.
** IN_PROGRESS can the AI bot help a person get started by accessing the same data as the screen the user is on? [2026-04-13 ai]
for instance, they navigate the user to resumes, behind the scenes they access resumes API and see that they dont' have any.  then the AI suggests adding one.
#+NOTE: [2026-04-13] Done. System prompt "Onboarding Help" section instructs AI to check current page context on greetings, query the corresponding tool, and suggest creating items if list is empty. Depends on page context (item 3) and =get_resumes= tool (item 2). Needs testing.
** DONE explore the cost benefit of using websockets for chat.
CLOSED: [2026-04-13 Sun]
Decision: deferred. SSE handles all current and planned features including elicitation via POST-back. WebSockets would require django-channels, ASGI deployment, Redis, Docker changes — significant infrastructure for marginal benefit.
** IN_PROGRESS AI usage for non-admin is scoped to the logged in user.  admin can see per user usage. [2026-04-13 frontend]
#+NOTE: [2026-04-13] Backend already scoped correctly. Added staff-only user dropdown to =settings/ai-spend= page — fetches user list, passes =user_id= param to API. "All users" default for admins. Non-admin sees own data only. Needs testing.


* Features — APPROVED (ready to work)

** TODO revamp scrapes/edit page                                   :APPROVED:
[[file:notes.org::*Revamp scrapes/edit page][Considerations → notes.org]]
Use more recent conventions. The spacing for the inputs looks good.

** TODO As a superuser I want to be able to manage user accounts   :APPROVED:
Check if this is complete.
[[file:notes.org::*Superuser account management][Considerations → notes.org]]
*** TODO decide on user roles and be able to manage them as admin

** TODO Security audit — multi-tenancy data isolation              :APPROVED:
[[file:notes.org::*Security audit — multi-tenancy data isolation][Considerations → notes.org]]
Multi-tenancy is a new feature and I want to make the data is secure.

** TODO Design plans for surfacing browser screenshots             :APPROVED:
[[file:notes.org::*Browser screenshot visibility (scrape debugging)][Considerations → notes.org]]
It might be just for debugging, but browser scraping is difficult and as a super user I want visibility.

* Features — PENDING APPROVAL

** TODO tool tips — where could they be useful?                    :PENDING_APPROVAL:
[PENDING APPROVAL] — Defer until shared components are built.
[[file:notes.org::*Tooltips][Considerations → notes.org]]
Favorites could be explained with tool tip.
Using HyperUI component patterns, write the tooltip as an argument into an Ember component.

** TODO Tackling deprecations                                      :PENDING_APPROVAL:
[PENDING APPROVAL] — Easy shims ready to go; full sweep needs approval.
[[file:notes.org::*Tackling Ember deprecations][Considerations → notes.org]]
Ember deprecations are numerous. Dagger is out of date. FastMCP may have an update.

** TODO Ian Finnis letter                                          :PENDING_APPROVAL:
[PENDING APPROVAL] — Post-alpha only.
[[file:notes.org::*Ian Finnis letter][Considerations → notes.org]]
Write a message to Ian Finnis asking permission to use his likeness for the webpage.
Point out it's a free service, explain open source in layman's terms.
Started writing while watching him and Tommy Fleetwood win the FedEx Cup in September.
Link to his social media or anything else he requests.
No money made — server costs are a loss. Blog at dougheadley.com, reach at doug@overcastsoftware.com.
New golfer, ~4 years. Social: https://www.instagram.com/finoEFC/

* Features — New / Unspecified

** DONE invited to create account
CLOSED: [2026-04-13 Sun]
Invitation model with one-time tokens. =POST /api/v1/accept-invite/= public endpoint.
Frontend =/accept-invite?token=...= route. Admin =/admin/invitations= page.
** DONE test email
CLOSED: [2026-04-13 Sun]
=POST /api/v1/test-email/= admin-only endpoint verifies SMTP config.
** DONE invite to create account from waitlist
CLOSED: [2026-04-13 Sun]
"Invite" button on admin waitlist page. Creates invitation, sends email, auto-removes waitlist entry.
** DONE signup page with dual mode (registration / waitlist)
CLOSED: [2026-04-13 Sun]
=/signup= shows full registration form when =REGISTRATION_OPEN=true=, waitlist form when closed.
Backend enforces — cookie manipulation cannot bypass.
** DONE password validation
CLOSED: [2026-04-13 Sun]
=AUTH_PASSWORD_VALIDATORS= added to Django settings. Min 8 chars, no common passwords, no numeric-only.
Validation on both =DjangoUserViewSet.create()= and =accept_invite=.
** TODO omarchy theme integration
We had looked at this before and found it feasible. Turn it into a feature.
[[file:notes.org::*Color Palette System][Omarchy integration roadmap → notes.org]]
** TODO scores: need an edit page to help with relationships that need to be manually set
** DONE =make up= should include AI — it's integral
CLOSED: [2026-04-13 Sun]
Removed =profiles: [browser]= from =browser-mcp= service. Now starts with default =make up=. Removed =up-ai= Makefile target.
** TODO Docker network error on =make up-ai=
#+BEGIN_SRC
Error response from daemon: failed to set up container networking: network ... not found
make: *** [Makefile:11: up-ai] Error 1
#+END_SRC
** TODO Claude for glint
** TODO /omarchy reinstall ollama
