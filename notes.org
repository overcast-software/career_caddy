#+title: CareerCaddy — Notes & Decision Records
:PROPERTIES:
:CREATED:  2026-04-08
:END:

* Agent Mistakes — Tally: 3                                        :IMPORTANT:
Track repeated agent errors so patterns are visible and accountable.

| # | Date | Agent | Mistake | Rule violated |
|---+------+-------+---------+---------------|
| 1 | 2026-04-14 | frontend | Compound bash: =cd ... && git diff= | One command per Bash call, use =-C= or absolute paths |
| 2 | 2026-04-15 | frontend | Jumped to coding before fully reading notes.org | "read notes, internalize the lessons" — read the full file before acting |
| 3 | 2026-04-16 | api | Used =cd= in Bash call for frontend git status | One command per Bash call, use =-C= or absolute paths |

* Curriculum gap analysis [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*Curriculum gap analysis][notes_archive.org]]. Phase 2 maturity assessment; 6 gaps added to todo.org.

* Pending Approval — Decision Considerations                       :IMPORTANT:
Decisions needed from the product owner before agents begin work.
Reply inline or update todo.org to unblock.

** Backups (export / import) [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*Backups (export / import)][notes_archive.org]]. DONE — xlsx export/import with skip-duplicates, superuser-only.

** Tooltips
[[file:todo.org::*tool tips][↑ todo.org]]
- Only one concrete example was given: the ★ favorites button.
- Proposal: build a shared =<Tooltip>= Glimmer component that accepts a =@text= arg and wraps any element; adopt the HyperUI tooltip pattern.
- Scope question: audit all ambiguous UI affordances first, or implement favorites tooltip only and expand later?

- Scrape show subnav buttons (Retry, Queue, Parse) would benefit from tooltips explaining what each does:
  - *Retry*: dispatches to browser-mcp for immediate scrape
  - *Queue*: sets status to =hold= for the poller to pick up
  - *Parse*: re-runs extraction on existing job content

*** Waiting on
Approve the shared-component pattern and confirm scope (favorites-only to start, or full audit).

** Revamp scrapes/edit page
[[file:todo.org::*revamp scrapes/edit page][↑ todo.org]]
- =feature/scrapes-improvements= is merged; this is now unblocked.
- "More recent conventions" — does this mean matching the existing =panel-card= / =field-label= / =field-input= patterns used on job-posts/edit and companies/edit?
  It actually looks pretty good.  the buttons should be below on show and not in subnav on scrape/show
  the scrapes/index page show full job posts and it's way to big for the table sometimes.
- Or is there a specific layout change in mind (e.g. side-by-side fields, inline status)?

*** Waiting on
Confirm the target convention. Agents will pattern-match against an existing edit page unless told otherwise.

** CSS maturation / HyperUI / dark mode / jade palette [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*CSS maturation][notes_archive.org]]. COMPLETED — 4 tracks, dark mode, jade palette, Tailwind-first.

** Superuser account management
[[file:todo.org::*As a superuser I want to be able to manage user accounts][↑ todo.org]]

- Currently no row-level ownership on the 33 domain models — any authenticated user can read any record.
- Multi-tenancy requires adding an =owner= FK on every model and filtering every queryset.
- Implementation options:
  - Django custom =get_queryset= on every ViewSet (surgical, low magic).
  - A shared mixin =OwnedQuerysetMixin= that enforces =owner=request.user= automatically.
- Superuser role: Django =is_staff= flag or a dedicated =role= field on the User model?
  is staff is fine
- This is a large API-first feature; frontend follows once the ownership model is defined.

*** Decision
:PROPERTIES:
:DECIDED:  [2026-04-08 product-owner]
:END:
- Multi-tenancy: APPROVED.
- Ownership model: use =is_staff= as the superuser flag.
- No new =owner= FK needed — leverage existing fields (confirmed below).

*** API notes [api]
No new FK column needed on any model. Every endpoint already has a user reference; the field name just varies.
Enforce at the queryset level per ViewSet using a class attribute =user_lookup= that names the ORM filter expression:

| ViewSet | Existing field | queryset filter |
|---|---|---|
| =CoverLetterViewSet= | =user= | =user_id=request.user.id= |
| =JobApplicationViewSet= | =user= | =user_id=request.user.id= |
| =ResumeViewSet= | =user= | =user_id=request.user.id= |
| =ScoreViewSet= | =user= | =user_id=request.user.id= |
| =SummaryViewSet= | =user= | =user_id=request.user.id= |
| =ProjectViewSet= | =user= | =user_id=request.user.id= |
| =JobPostViewSet= | =created_by= | =created_by_id=request.user.id= |
| =QuestionViewSet= | =created_by= | =created_by_id=request.user.id= |
| =AnswerViewSet= | none direct | =question__created_by_id=request.user.id= |
| =ScrapeViewSet= | none direct | =job_post__created_by_id=request.user.id= |
| =CompanyViewSet= | none | intentionally shared — no filter |
| Skill, Status | none | global lookup tables — no filter |
| Pivot tables (ResumeSKill etc.) | none | scoped via parent endpoint only |

Mixin approach (one place, covers most ViewSets):
#+BEGIN_SRC python
class OwnedQuerysetMixin:
    user_lookup = "user_id"          # override per ViewSet where needed
    def get_queryset(self):
        qs = super().get_queryset()
        if self.request.user.is_staff:
            return qs
        return qs.filter(**{self.user_lookup: self.request.user.id})
#+END_SRC

ViewSets with a different field name just set =user_lookup = "created_by_id"= or ="question__created_by_id"=.
No model migrations. No new columns.

*** Waiting on
~~Multi-tenancy approved?~~ APPROVED. Proceed after security-audit branch confirms the full list.

** Waiting list sign-up [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*Waiting list sign-up][notes_archive.org]]. DONE — standalone POST /api/v1/waitlist/, AllowAny, Django admin review.

** Email — account recovery and library [ARCHIVED 2026-04-13]
Archived to [[file:notes_archive.org::*Email][notes_archive.org]]. DONE — django.core.mail + SMTP, password reset, welcome/waitlist emails, 15 tests.

** Lazy load child resources [ARCHIVED 2026-04-13]
Archived to [[file:notes_archive.org::*Lazy load child resources][notes_archive.org]]. DONE — 12 routes trimmed, async relationships, kept index includes to avoid N+1.

** Pydantic-AI toolsets — dynamic tool scoping
:PROPERTIES:
:CREATED:  [2026-04-10]
:END:
[[file:todo.org::*MCP exposure][↑ todo.org]]

*** Problem
All pydantic-ai agents connect to =career_caddy_server.py= via =MCPServerStdio=, spawning a full Python subprocess per agent run. Every agent gets the same 14 tools regardless of what it actually needs. This adds latency, memory overhead, and prevents fine-grained tool scoping.

*** Solution: =CareerCaddyToolset(AbstractToolset)=
A single custom toolset class with a =scope= parameter that selects which =lib/api_tools.py= functions to expose. No subprocess, no MCP protocol overhead — direct in-process calls.

#+BEGIN_SRC python
from lib.toolsets import CareerCaddyToolset, CareerCaddyDeps

# Scoped to just what this agent needs
agent = Agent(
    "openai:gpt-4o-mini",
    deps_type=CareerCaddyDeps,
    toolsets=[CareerCaddyToolset(scope="scoring")],
)
result = await agent.run(prompt, deps=CareerCaddyDeps(api_token=token))
#+END_SRC

*** Deps type
#+BEGIN_SRC python
@dataclass
class CareerCaddyDeps:
    api_token: str
    base_url: str = "http://localhost:8000"
#+END_SRC

The toolset builds an =ApiClient(deps.base_url, deps.api_token)= from =ctx.deps= at call time and passes it to each =api_tools.*= function.

*** Scopes
| Scope | Tools | Use case |
|---|---|---|
| =all= | All 19 functions | Full access |
| =career_caddy= | Original 14 | Drop-in replacement for current MCPServerStdio |
| =job_discovery= | company search/create, job post create/find/search | Email pipeline agent |
| =scoring= | get_job_posts, get_career_data, score_job_post, get_scores | Scoring agent |
| =application_tracking= | job application CRUD, update_job_post, find_by_link | Status update agent |
| =scrape_management= | create_scrape, get_scrapes, update_scrape | Scrape queue agent |

Custom tool lists also accepted: =CareerCaddyToolset(scope=["score_job_post", "get_career_data"])=

*** Implementation: =ai/lib/toolsets.py= (new file)
Contains:
- =CareerCaddyDeps= dataclass
- =TOOL_REGISTRY= — dict mapping tool names to =api_tools.*= functions
- =SCOPES= — dict mapping scope names to sets of tool names
- =_make_tool_wrapper(fn)= — builds a =RunContext=-aware wrapper that preserves the original function's signature (minus the =api= param) for pydantic-ai schema generation
- =CareerCaddyToolset(AbstractToolset[CareerCaddyDeps])= — delegates to internal =FunctionToolset=
- Factory functions: =job_discovery_toolset()=, =scoring_toolset()=, etc.

*** Key challenge: signature preservation
Pydantic-ai uses =inspect.signature()= to build JSON schemas for tool parameters. The wrapper must copy the original function's parameter list (minus =api: ApiClient=) and construct a new =__signature__=. Same pattern FastMCP uses internally for =@server.tool()=.

*** Migration path
1. Create =ai/lib/toolsets.py=
2. Update =career_caddy_agent.py=: replace =MCPServerStdio= with =CareerCaddyToolset(scope="career_caddy")=, add =deps_type=CareerCaddyDeps=
3. Update =job_email_to_caddy.py=: indirect — it calls =add_job_post()= which uses the agent
4. =email_classifier_agent.py=: no change (uses email server only)
5. =job_extractor_agent.py=: no change (pure LLM, no tools)

*** Security: browser credentials are unreachable
- =lib/api_tools.py= imports only =httpx=, =json=, =pydantic= — zero knowledge of =secrets.yml= or browser automation
- =lib/toolsets.py= imports only from =lib/api_tools.py= and =pydantic_ai= — MUST NOT import from =lib/browser/*= or =mcp_servers/browser_server.py=
- =CareerCaddyDeps= holds only =api_token= + =base_url= — no credential paths
- No tool in the registry can read =secrets.yml=, trigger a browser login, or access notmuch email
- Browser/email capabilities live exclusively in =browser_server.py= and =email_server.py=, which are never imported by the toolset
- This is the same isolation model as =public_server.py= — enforced by import boundaries, not runtime checks

*** What stays the same
- =career_caddy_server.py= / =public_server.py= / =gateway.py= — still serve external MCP clients
- =lib/api_tools.py= — the single source of truth for tool implementations
- Email and browser MCP servers — separate tool sources, outside this scope

*** Files
| File | Action |
|---|---|
| =ai/lib/toolsets.py= | Create — CareerCaddyToolset + deps + scopes |
| =ai/lib/api_tools.py= | No change — already the right shape |
| =ai/agents/career_caddy_agent.py= | Migrate MCPServerStdio → CareerCaddyToolset |
| =ai/agents/job_email_to_caddy.py= | Update add_job_post call to pass deps |

** MCP deployment at mcp.careercaddy.online
:PROPERTIES:
:CREATED:  [2026-04-10]
:END:
[[file:todo.org::*MCP exposure][↑ todo.org]]

*** Architecture decision
Separate =public_server.py= — NOT the existing gateway. The gateway aggregates email + browser + caddy tools for local use. The public server exposes only career-data tools (21 total) behind per-client API key auth.

#+BEGIN_SRC
MCP Client → mcp.careercaddy.online (Caddy, TLS) → public_server.py (FastMCP) → Django API → PostgreSQL
#+END_SRC

*** Auth: API key pass-through
- FastMCP's =TokenVerifier= base class validates =jh_*= API keys by calling =GET /api/v1/me/= on the Django API.
- On success, the raw token is stored in =AccessToken.token= and forwarded on every downstream HTTP call.
- The Django API resolves user identity from the key — the MCP server never touches the DB.
- Each tool reads the token via =get_access_token()= from FastMCP's dependency injection.
- =get_current_user= tool: returns the authenticated user's profile so agents know who they act as.
- plan for AI integration chat on the frontend
- plan for AI integration via slack
**** I have a branch at  /home/oldbones/Network/syncthing/Projects/career_caddy_ai/lib/tiger-agents-for-work
It is capable of doing chat in slack
however it's a hack on the original, and I'd like to not need to depend on a fork of that tiger project
The agent should work on behalf of the person talking to it and no other.  It serves multiple users by using the correct api key
- AI integration should have authorization access
- By leveraging toolsets,   the AI should be constrained by user account auth.  some users will only get readonly for example
**** I have  /home/oldbones/Network/syncthing/Projects/career_caddy_ai/run_email_web.py that I'd like to rework into and application to test mcp/careercaddy.online
**** a chat client live in a sidebar on the right hand side of the web appliation although a full screen can be found at /caddy
**** The chat can only be accessed by authed users with access and login from the front end will inform the agent on which user to act for.

*** Tools exposed (21)
14 ported from =career_caddy_server.py=:
- Companies: create, find_by_name, search, get
- Job posts: create_with_company_check, find_by_link, search, get, update
- Applications: create, get, get_for_job_post, update
- Career data: get

7 new:
- =get_current_user= — authenticated user profile
- =create_scrape= — queue URL with =status="hold"= for later processing
- =get_scrapes=, =update_scrape= — scrape CRUD
- =score_job_post= — POST score against career data or specific resume (async, returns pending)
- =get_scores=, =get_scores_for_resume= — check scoring results

*** "hold" status for scrapes
API change: =ScrapeViewSet.create= accepts =status="hold"= and skips the =SCRAPING_ENABLED= check and browser dispatch. Creates the record and returns 201 immediately. Agents use this to queue URLs for a separate scraping process.

*** Security invariants
- =public_server.py= does NOT import =email_server=, =browser_server=, =gateway=, or =lib/browser/*=
- Docker service does NOT mount =secrets.yml= or mail directories
- No =CC_API_TOKEN= env var on the container — all auth from clients
- Rate limiting via Caddy's =rate_limit= directive

*** OAuth roadmap (Phase 2 — not yet implemented)
For in-app chat where the agent acts as the logged-in user:
1. Add =django-oauth-toolkit= to the API (authorization server)
2. Implement PKCE flow for the Ember SPA
3. Use FastMCP's =MultiAuth= to accept both API keys and OAuth tokens
4. Frontend chat widget initiates PKCE, gets access token, passes to MCP
Separate workstream. API key auth covers all current use cases.

*** Agent identity awareness
:PROPERTIES:
:CREATED:  [2026-04-11]
:END:
Two paths for the agent to know the user's identity:

**** Web chat (chat_server.py) — JWT pass-through (Option C)
- Frontend sends JWT in =Authorization: Bearer= header to =/api/v1/chat/=
- Django proxy validates the JWT, then forwards the *same token* to the chat service
- No temporary API keys created — eliminates the DB write per message overhead
- Chat service calls =GET /api/v1/me/= with the forwarded token to fetch user profile
- Profile (name, email, linkedin, github) is injected into the system prompt
- Same token used for all downstream tool calls via =ApiClient=
- System prompt explicitly instructs the agent to use this data and never deny access
- Name fallback chain: =first_name last_name= → =username= → =email= → "authenticated but no profile"
- Chat service accepts both JWTs and =jh_*= API keys — the API's auth stack handles either

**** Public MCP server (public_server.py)
- FastMCP =instructions= parameter tells MCP clients to call =get_current_user= at session start
- =get_current_user= tool returns the authenticated user's full profile from the token claims
- No system prompt injection needed — the MCP client's own LLM handles the instructions
- Auth: =jh_*= API keys provisioned by the user (long-lived, for CLI/Slack/third-party apps)

**** Key insight
The web chat injects profile into the system prompt (server-side). The MCP server provides
instructions for the client to fetch it (client-side). Both paths achieve the same result:
the agent knows who the user is from the first message.

Two auth token types serve different clients:
- JWT: browser sessions (short-lived, auto-refreshed by the SPA)
- API key (=jh_*=): MCP clients, Slack, CLI, third-party apps (long-lived, user-provisioned)

*** Chat UI architecture
:PROPERTIES:
:CREATED:  [2026-04-11]
:END:

**** Grid-based sidebar
The chat sidebar is a CSS grid area (=course-chat=), not a fixed overlay. It pushes the subnav
and main content exactly like the left sidebar pushes from the other side. Grid definitions:
- =course--chat-open=: adds =chat= column at =24rem=
- =course--sidebar-open.course--chat-open=: three columns (sidebar + main + chat)
- Mobile: chat takes full width, hides main content

Opening one sidebar closes the other (mutual exclusion via =MainApplication= actions).

**** Shared conversation state
Both the sidebar panel and the =/caddy= full-page route render =<Chat::Panel />=, which reads
from the singleton =chat= service. =sidebarOpen= state lives on the service so =MainApplication=
and =Chat::Sidebar= can coordinate. Messages, streaming state, and conversation ID persist across
route transitions within the same browser tab.

**** Footer toggle
The floating bubble button was replaced with an inline "Chat with AI" link in the footer =<nav>=.
Hidden for guests. Hidden when the chat sidebar is already open.

*** Chat markdown + SPA link navigation
:PROPERTIES:
:CREATED:  [2026-04-12]
:END:

**** Markdown rendering
Assistant messages in =<Chat::Message>= render through =<MarkdownToHtml>= (ember-cli-showdown).
User messages remain plain text. Compact =.chat-markdown= CSS (tighter spacing than =.markdown-content=)
with accent-colored links and dark mode support.

**** SPA-aware links
=chat/message.js= intercepts clicks on =<a href>= inside assistant messages. Relative paths
(e.g. =/companies/42/job-applications=) route through =this.router.transitionTo()= — no page reload.
External links (=http=, =mailto:=) pass through normally. This lets the AI link users directly
to resources: "Check your [Disney applications](/companies/42/job-applications)".

**** Auth bouncer messages
Instead of raw HTTP errors, unauthenticated/guest chat attempts show friendly AI-style responses:
- 401 (not logged in): "I'd love to help, but I need you to log in first. Head over to the [login page](/login)..."
- 403 (guest account): "I can see you're browsing as a guest — welcome! [Sign up or log in](/login)..."
Both use markdown links that navigate via the Ember router.

*** Flash messages standard
:PROPERTIES:
:CREATED:  [2026-04-11]
:END:

**** Severity types
| Type | Use for | Example |
|------|---------|---------|
| =success= | Action completed | "Company saved." |
| =danger= | Action failed | "Failed to save company." |
| =warning= | Permissions, caution | "This feature requires a full account." |
| =info= | Status, progress | "Generating cover letter..." |

Retired: =alert=, =error=, =warn= — replaced across all 26 files.

**** Message style
- Sentence case, end with period
- Success: ="{Thing} saved."=, ="{Thing} deleted."=, ="{Thing} created."=
- Failure: ="Failed to save {thing}."=
- No "successfully" filler
- 403: no local flash — adapter handles globally with =sticky: true=

**** Save pattern
All saves use async/await with try/catch and 403 guard:
#+BEGIN_SRC javascript
try {
  await record.save();
  this.flashMessages.success('Thing saved.');
} catch (error) {
  if (error?.status !== 403) {
    this.flashMessages.danger('Failed to save thing.');
  }
}
#+END_SRC

*** Deployment
- =docker-compose.prod.yml=: new =mcp= service using AI image, port 8030, depends on API healthcheck
- Caddy reverse proxy: =mcp.careercaddy.online { reverse_proxy localhost:8030 }=
- DNS: A record for =mcp.careercaddy.online= → VPS IP
- CI: build =ai/Dockerfile= with =INSTALL_CAMOUFOX=false= and push to GHCR

*** Prod scraping — browser-mcp removed from VPS
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:
Browser scraping is too heavy for the VPS (4 CPU, shared with other apps). Camoufox caused
sluggishness and Toptal timed out after 60s.

*Decision:* browser-mcp removed from =docker-compose.prod.yml=. =USE_MCP_BROWSER_AGENT=False=.
Scrapes created with =status: "hold"= sit in the DB until picked up by the hold-poller running
on a local device (Raspberry Pi or desktop). GHCR AI image built without Camoufox (slim).

*Standalone Pi project:* =~/Sandbox/caddy-browser-pi/= — self-contained copy of hold-poller
source files, own Dockerfile with Camoufox + GTK libs, docker-compose pulls no GHCR image.
Builds locally on the Pi.

*Prod compose services:* db, api, frontend, mcp (public), chat.

*History:* browser-mcp was added, tested, confirmed working (libgtk-3.so.0 fixed by
adding =INSTALL_CAMOUFOX=true= to Dagger publish), then removed same day due to VPS perf.

*Optimization strategies (future):*
1. *On-demand browser-mcp* — don't run it 24/7. Use =docker compose up browser-mcp= only when scraping, or set =restart: "no"= and have the API start it via Docker API before dispatching. Scraping is infrequent; the browser idles 99% of the time.
2. *Merge chat + browser-mcp into one process* — both are Python/FastMCP on the AI image. A single service could host both servers on different ports (or one port with path routing). Saves ~256MB+ RAM from the duplicate Python runtime and Camoufox memory.
3. *Slim AI image variants* — build two images: =ai-lite= (no camoufox, for mcp + chat) and =ai-browser= (with camoufox, for browser-mcp only). The =INSTALL_CAMOUFOX= build arg already supports this.
4. *Move browser-mcp off the VPS* — run it locally or on a beefier machine. The hold-poller pattern already supports this (queue on VPS, scrape locally). Only direct =/scrapes/new= would need the VPS browser.
5. *Scale down replicas* — reduce =GUNICORN_WORKERS= from 2 to 1 if traffic is low. The chat and MCP services are single-worker already.
6. *Swap Camoufox for lightweight scraping* — for sites that don't need JS rendering or anti-bot evasion, use =httpx= + BeautifulSoup directly in the API (Tier 0 in the scrape profile system). Reserve Camoufox for login-wall sites only.

** Security audit — multi-tenancy data isolation
[[file:todo.org::*Security audit][↑ todo.org]]

- The audit scope: all 33 domain models, every ViewSet =.get_queryset()=, every custom action endpoint.
- Output: a report listing which endpoints are currently unscoped (any user can read any record) and a remediation plan.
- Should run before or in parallel with the superuser account management feature.
- Low risk to start: the audit branch only reads code and writes a report; no production changes until approved.

*** API notes [api]
- Known unscoped endpoints right now (from reading =views.py=):
  - =CompanyViewSet.list= — returns all companies for all users (companies are shared by design, but verify this is intentional).
  - =JobPostViewSet.list= — returns all job posts regardless of =created_by=.
  - =CoverLetterViewSet.list= — already scoped to =user_id=request.user.id=; safe.
  - =ResumeViewSet.list= — scoped via user FK; safe.
  - =SummaryViewSet.list= — scoped via =user_id=; safe.
  - =AnswerViewSet= — no scoping; any user can read any answer.
  - =QuestionViewSet= — no scoping visible; needs check.
  - Custom =career-data= endpoint — scoped to authenticated user (safe, but a path parameter lets any user request any =user_id=; soft risk).
- Automated tool: =bandit= is already in CI but doesn't catch query-scoping gaps; the audit is a code-read + test exercise.
- Output format: a markdown report committed to =api/SECURITY_AUDIT.md=, no code changes in the same branch.

*** Waiting on
~~Approve the audit branch (read-only, report only). Code changes in a separate branch after review.~~ APPROVED.
#+NOTE: [2026-04-15] Audit complete — report at =api/SECURITY_AUDIT.md=. 5 critical (Education, Certification, Experience, Description, Project ViewSets + career_data endpoint), 2 high (DjangoUserViewSet detail actions), 2 medium (_build_included filter, ScrapeViewSet update/destroy). Remediation plan included with 7 prioritized phases.

** Dockerfile caching [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*Dockerfile caching][notes_archive.org]]. DONE — dependency-first caching in all 3 Dockerfiles + Dagger pipeline.

** Tackling Ember deprecations
[[file:todo.org::*Tackling deprications][↑ todo.org]]
- Deprecation warnings are already visible in test output (=ember-data:deprecate-legacy-imports= for =StringTransform=, =DateTransform=, =BooleanTransform=).
- Easy fixes (zero risk): create =app/transforms/string.js=, =date.js=, =boolean.js= re-exporting from =@ember-data/serializer/transform=. Three files, three lines each.
- Larger sweep: legacy =@ember/object= patterns, old serializer inheritance — these need more careful branch-by-branch work.

*** Waiting on
Approve starting with the transform shims (isolated, safe). Full sweep can be a follow-on branch.

** Job-application breadcrumb UI [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*Job-application breadcrumb UI][notes_archive.org]]. DONE — StatusSteps component with numbered circles, checkmarks, connecting lines.

** SPA analytics with Plausible [ARCHIVED 2026-04-11]
Archived to [[file:notes_archive.org::*SPA analytics with Plausible][notes_archive.org]]. COMPLETE — self-hosted at analytics.careercaddy.online, auto SPA tracking.

** Fonts and buttons from "under construction" page
[[file:todo.org::*fonts and buttons from "under construction"][↑ todo.org]]
- You liked the font and two-colour button style from the generated under-construction page.
- That file is at =file:///home/oldbones/Projects/career_caddy_frontend/under-construction.html=.
- Plan: read the file, extract font family, colour tokens, and button classes, then apply them to =app.css= as the foundation for the CSS overhaul and dark mode palette.
- This should happen *before* the CSS maturation tracks above — it sets the design tokens everything else inherits.
#+NOTE: [2026-04-09 prune] CSS tracks are complete. Dark mode palette was drawn from under-construction.html. Font/button adoption is the remaining piece.

** User settings page
[[file:todo.org::*user settings page][↑ todo.org]]
- Content: update profile (name, email, password), dark mode toggle, colour palette picker.
- ~~Blocked on CSS consolidation and shared components (CSS maturation tracks 1–3).~~ CSS tracks complete [2026-04-09]. Unblocked.
- ~~Dark mode toggle has no effect until the dark mode track is complete.~~ Dark mode + ThemeService landed.

*** Design decisions
:PROPERTIES:
:CREATED:  [2026-04-11]
:END:
- =/settings= with two subnav tabs: *Profile* (default) and *Data* (import/export).
- Career Data stays as its own top-level sidebar link — it's a resource view, not an action.
- Import/export xlsx are actions, so they live under Settings > Data, not in the main nav.
- Settings cog link goes in the sidebar bottom section near the dark mode toggle.
- Follows the admin page pattern: parent route loads currentUser, child routes inherit model.
- Profile fields exposed: first_name, last_name, email, phone, linkedin, github, address. =links= JSON is read-only for v1.

*** API notes [api]
- Profile update: =PATCH /api/v1/users/{id}/= already exists and supports =first_name=, =last_name=, =email= — no new endpoint needed.
- New fields to expose: linkedin, github, address, links (already on Profile model, not yet in serializer).
- =DjangoUserSerializer.to_resource()= needs to include the 4 new profile fields in attributes.
- =DjangoUserSerializer.parse_payload()= needs to accept the 4 new profile fields.
- =DjangoUserViewSet._upsert()= needs to persist linkedin/github/address/links alongside phone.
- Password change: add =POST /api/v1/users/{id}/change-password/= (future — not in v1).
- Dark mode / palette preferences: store as JSON on the =Profile= model (future — not in v1).

*** Waiting on
~~Stays blocked until CSS maturation tracks 1–3 are approved and in progress.~~ UNBLOCKED [2026-04-09]. APPROVED [2026-04-11].

** Resume template review
[[file:todo.org::*resume template - review][↑ todo.org]]
- Resume show and edit were among the first pages built; likely cruft and inconsistent patterns.
- Review scope: compare against current conventions (RouteLayout, field-label/field-input, Panel::Card), identify divergences, refactor in small commits.
- ~~Blocked on shared component refactor (CSS maturation track 2) so there are stable components to refactor towards.~~ Shared components landed (EmptyState, Buttons::Favorite). Unblocked.

*** Waiting on
~~Stays blocked until shared components exist.~~ UNBLOCKED [2026-04-09]. Ready for approval.

** Browser screenshot visibility (scrape debugging)
[[file:todo.org::*Design plans for surfacing browser screenshots][↑ todo.org]]
- Camoufox captures screenshots during scraping; currently only visible in the AI layer logs.
- A superuser debug view would surface the last screenshot per scrape via the API and a frontend page.  may be more in the future
- Security: screenshots must be served behind a superuser-only endpoint, not a public path; no CDN caching.
- Implementation: the AI layer saves the screenshot path or base64; the API exposes it on the scrape resource; frontend renders it on the scrape show page for admins.

*** API notes [api]
- The =Scrape= model (=api/job_hunting/models/scrape.py=) would get a =screenshot_path= or =screenshot_b64= field.
- Simpler path: store a file path; serve via a =GET /api/v1/scrapes/{id}/screenshot/= endpoint that reads the file and returns =image/png= with =IsAdminUser= permission — no base64 bloat in JSON responses.
- The AI layer already has the screenshot in memory after a Camoufox call; it just needs to save it to a shared path accessible by the API container (i.e. a shared Docker volume).
- Infrastructure change: mount a shared =screenshots/= volume between the =ai= and =api= services in =docker-compose.yml=.

*** Waiting on
Approve for superuser-gated debug use, or defer until post-alpha?
APPROVED There may be many screenshots, not just the final one.  Puting in an image path is my choice with `/api/v1/scrapes/{id}/screenshots` (plural)

** Agent logfire
https://pydantic.dev/docs/logfire/integrations/llms/pydanticai/
https://pydantic.dev/docs/logfire/integrations/web-frameworks/django/
** make doctor
It would be nice to audit the needs for running an application for a user trying to set things up.
Make a clear plan how to bring this feature about.

** Ian Finnis letter
[[file:todo.org::*Ian Finnis letter][↑ todo.org]]
Deferred until official alpha phase. Draft is captured in todo.org.
Move this entry to notes.org when alpha begins and send then.

* Linting techniques                                                :IMPORTANT:
:PROPERTIES:
:CREATED:  [2026-04-11]
:END:

** Frontend linting (Ember)
The frontend has four lint targets run in parallel via =npm run lint=:
- =lint:js= — ESLint (=eslint . --cache=)
- =lint:hbs= — ember-template-lint
- =lint:css= — Stylelint
- =lint:format= — Prettier (=prettier . --cache --check=)

*** Running lint
#+begin_src bash
npm --prefix /path/to/frontend run lint        # all four in parallel
npm --prefix /path/to/frontend run lint:js     # just ESLint
npm --prefix /path/to/frontend run lint:hbs    # just template lint
npm --prefix /path/to/frontend run lint:format # just prettier check
#+end_src

*** Fixing prettier issues
Prettier must be run from within the frontend submodule context so that =prettier-plugin-ember-template-tag= resolves correctly. Running prettier from the repo root will fail because the root =noop.js= can't find the plugin.

#+begin_src bash
# Check what's wrong:
npm --prefix /path/to/frontend run lint:format

# Auto-fix (pass files after -- --write):
npm --prefix /path/to/frontend run lint:format -- --write /absolute/path/to/file.hbs
#+end_src

*** Key gotcha
Do NOT run =npx prettier= from the repo root — it picks up the root config and fails to resolve the Ember template tag plugin. Always use =npm --prefix frontend run lint:format=.

** API linting (Django)
Single linter: ruff.

#+begin_src bash
uv run --directory /path/to/api ruff check .         # check all
uv run --directory /path/to/api ruff check path/to/file.py  # check specific files
#+end_src

Ruff must be run via =uv run= because the project uses a uv-managed virtualenv. Always lint before pushing (per CLAUDE.md).

* Color Palette System
:PROPERTIES:
:CREATED:  [2026-04-11]
:END:

** Architecture
Two independent dimensions: *mode* (light/dark) and *palette* (indigo/blue/jade/rose/amber/violet).
Applied via =data-theme= and =data-palette= attributes on =<html>=.
ThemeService stores both in localStorage (=theme-mode=, =theme-palette=).
Settings > Appearance tab lets users pick palette + toggle dark mode.

** How to add a new palette

1. Pick a color family (Tailwind reference or custom). You need shades 50-900.
2. Convert hex to space-separated RGB: =printf "%d %d %d\n" 0xRR 0xGG 0xBB=
3. Add a CSS block in =frontend/app/styles/app.css= after existing palette blocks:
   #+begin_src css
   [data-palette="teal"] {
     --accent-50:  240 253 250;
     --accent-100: 204 251 241;
     /* ... shades 200-800 ... */
     --accent-900: 19 78 74;
     --accent: rgb(var(--accent-500));
     --tee-bg: rgb(var(--accent-200));
     --page-bg: #f0fdfa;
   }
   #+end_src
4. Add the name to =PALETTES= array in =frontend/app/services/theme.js=.
5. Add swatch colors to =SWATCH_COLORS= in =frontend/app/controllers/settings/appearance.js=.
6. No template changes needed — all templates use =accent-*= classes.
7. Test in both light and dark modes.

** Omarchy OS theme integration (roadmap)
Concept: when =omarchy-theme-set tokyo-night= runs, CareerCaddy palette updates to match.

*** How omarchy themes work
- 19 stock themes at =~/.local/share/omarchy/themes/=
- Each has =colors.toml= with 22 tokens: =accent=, =background=, =foreground=, =cursor=, =color0-15=
- Template engine processes =.tpl= files, provides ={{ accent }}=, ={{ accent_rgb }}=, ={{ accent_strip }}=
- Hooks at =~/.config/omarchy/hooks/theme-set= fire after theme change

*** Integration path: API preference sync (works for production)
=~/.config/omarchy/hooks/theme-set= calls CareerCaddy API to store accent preference:
#+begin_src bash
#!/bin/bash
ACCENT=$(grep '^accent' ~/.config/omarchy/current/theme/colors.toml | head -1 | cut -d'"' -f2)
curl -sf -X PATCH "${CC_API_URL:-http://localhost:8000}/api/v1/users/me/preferences/" \
  -H "Authorization: Api-Key $CC_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"accent\": \"$ACCENT\"}"
#+end_src

*** Prerequisites (not yet implemented)
- API: =PATCH /api/v1/users/me/preferences/= endpoint
- API: store accent preference in user profile JSON
- Frontend: ThemeService reads user preference on login, applies as custom palette
- Frontend: derive 50-900 scale from single hex (color math or lookup table)

* Email features — invitations, signup, test email
:PROPERTIES:
:CREATED:  [2026-04-13]
:END:

** Invitation system
- =Invitation= model: email, token (=secrets.token_urlsafe(32)=), created_by FK, created_at, accepted_at, expires_at (7 days)
- Properties: =is_valid=, =is_expired=, =is_accepted=
- =InvitationViewSet= (admin-only): list, create (sends email, auto-removes waitlist entry), destroy (revoke)
- =POST /api/v1/accept-invite/= (public): validates one-time token → creates user + profile → marks accepted → sends welcome email
- Frontend: =/accept-invite?token=...= public route, =/admin/invitations= table with status pills + revoke

** Signup dual-mode page
- =/signup= route checks =health.registrationOpen= (from healthcheck API response)
- Registration open: full form (first name, last name, email, username, password, confirm) → =POST /api/v1/auth/register/=
- Registration closed: email-only waitlist form → =POST /api/v1/waitlist/=
- Security: =DjangoUserViewSet.create()= checks =settings.REGISTRATION_OPEN= server-side; staff users bypass the gate
- Cookie/sessionStorage manipulation cannot bypass — backend is the authority

** Registration control
- =REGISTRATION_OPEN= env var (default =false=)
- Exposed in healthcheck response: ={"healthy": true, "bootstrap_open": ..., "registration_open": ...}=
- Frontend =HealthService= caches in =sessionStorage('cc:registration-open')=

** Password validation
- =AUTH_PASSWORD_VALIDATORS= in settings.py: UserAttributeSimilarity, MinimumLength (8), CommonPassword, NumericOnly
- Enforced in: =DjangoUserViewSet.create()=, =_create_user_from_data()= (used by =accept_invite=)
- =REGISTRATION_OPEN = True= in =TESTING= block so tests can register

** Test email
- =POST /api/v1/test-email/= — admin-only, DRF =@api_view= + =@permission_classes=
- Defaults to admin's own email if none provided
- Template: =test_email.txt= with timestamp

** Test coverage
- =test_invitation.py=: 27 tests (model, create, accept, list, destroy)
- =test_test_email.py=: 4 tests (admin sends, default email, non-admin 403, unauth 401)

* AI Chat — Architecture Reference
:PROPERTIES:
:CREATED:  [2026-04-13]
:END:
New agents: read this section to understand how AI chat works end-to-end.

** Overview
Chat is a three-layer streaming system: Ember frontend → Django API proxy → Pydantic-AI chat server.
The user types a message, it flows through all three layers, and the response streams back as SSE events.
The AI agent has access to 27 tools that CRUD the user's career data via the same API.

** Message flow (end-to-end)
#+begin_src
User types in frontend (Chat::Panel textarea)
  ↓
ChatService.sendMessage(text)                    [frontend/app/services/chat.js]
  - adds user message + empty assistant placeholder to @tracked messages array
  - calls session.ensureFreshToken(90) to ensure valid JWT
  - POST ${baseUrl}chat/ with JSON body:
    {message, history, conversation_id, page_context}
    Authorization: Bearer <jwt>
  ↓
chat_proxy(request)                              [api/job_hunting/api/chat.py]
  - validates JWT via DRF JWTAuthentication
  - extracts raw token string from Authorization header
  - builds payload: {message, token, history, conversation_id, page_context}
  - streams POST to http://localhost:8031/chat via httpx.Client.stream()
  - returns StreamingHttpResponse (text/event-stream)
  ↓
chat(request)                                    [ai/mcp_servers/chat_server.py]
  - _fetch_user_profile(token): GET /api/v1/me/ → name, email, linkedin, github
  - _build_agent(user_profile, page_context): creates pydantic-ai Agent
    with dynamic system prompt (profile + page context injected)
  - CareerCaddyDeps(api_token=token, base_url) passed to agent.run_stream()
  - agent streams text chunks + makes tool calls as needed
  - each tool uses the same JWT for API calls → data is scoped to the user
  ↓
SSE event stream yields back through all layers:
  data: {"type": "text", "content": "chunk..."}       ← partial text
  data: {"type": "navigate", "url": "/resumes"}       ← programmatic navigation
  data: {"type": "done", "content": "full text",      ← final response
         "conversation_id": "uuid", "usage": {...}}
  data: {"type": "error", "content": "error msg"}     ← on failure
  ↓
Frontend SSE reader accumulates text chunks, updates last message,
handles navigate events, stores conversation_id for multi-turn.
#+end_src

** Key files
| File | Layer | Purpose |
|------|-------|---------|
| =frontend/app/services/chat.js= | Frontend | Message state, SSE reader, page context tracking |
| =frontend/app/components/chat/panel.js= + =.hbs= | Frontend | Chat UI: input, send, message list, streaming indicator |
| =frontend/app/components/chat/message.js= + =.hbs= | Frontend | Message rendering, markdown, SPA links, elicitation buttons |
| =frontend/app/components/chat/sidebar.js= + =.hbs= | Frontend | Grid-based sidebar wrapper (=course-chat= CSS area) |
| =frontend/app/components/main-application.js= | Frontend | Route change tracking → =chat.currentPage= |
| =api/job_hunting/api/chat.py= | API | Django proxy: JWT validation, payload forwarding, SSE streaming |
| =ai/mcp_servers/chat_server.py= | AI | Starlette server: agent creation, system prompt, SSE generation |
| =ai/agents/agent_factory.py= | AI | Agent registry: =get_agent("chat")= returns configured Agent |
| =ai/lib/toolsets.py= | AI | =CareerCaddyToolset= + =TOOL_REGISTRY= + =SCOPES= |
| =ai/lib/api_tools.py= | AI | 27 tool functions (all return JSON strings via =ApiClient=) |
| =ai/lib/history.py= | AI | =truncate_message_history=, =sanitize_orphaned_tool_calls= |
| =ai/lib/usage_reporter.py= | AI | Fire-and-forget POST to =/api/v1/ai-usages/= |

** Authentication
- Frontend → API: JWT (=Authorization: Bearer <token>=, 60-min lifetime, auto-refresh)
- API proxy → Chat server: same JWT forwarded in JSON body as =token= field
- Chat server → API (profile fetch): =Bearer <token>= header on =GET /api/v1/me/=
- Chat server → API (tool calls): same token via =CareerCaddyDeps.api_token=
- All data is scoped to the authenticated user through the JWT — the chat server never stores tokens

** System prompt structure
The system prompt in =chat_server.py= is built dynamically per request. Sections:
1. *Role & rules* — identity, tool usage rules, company blocklist, resume counting
2. *Frontend URLs* — route map for all resources + markdown link requirements
3. *Navigation* — =<!-- navigate:/path -->= marker format
4. *Elicitation* — JSON action button format
5. *Onboarding* — proactive data checks on greetings
6. *User Profile* — injected via ={user_profile}= placeholder
7. *Current Page* — appended dynamically when =page_context= is present

** Agent configuration
Registered in =agent_factory.py= as ="chat"= role:
- Model: =openai:gpt-4o-mini= (override via =CHAT_MODEL= env var)
- Deps: =CareerCaddyDeps= (=api_token= + =base_url=)
- Toolset: =CareerCaddyToolset(scope="all")= — all 27 tools
- History processors: =truncate_message_history= (20k token budget) + =sanitize_orphaned_tool_calls=
- System prompt: empty at registration — =chat_server.py= injects the full prompt at runtime

** Tools available to chat (27 total, =scope="all"=)
| Category | Tools |
|----------|-------|
| Companies (4) | =create_company=, =find_company_by_name=, =search_companies=, =get_companies= |
| Job posts (5) | =create_job_post_with_company_check=, =find_job_post_by_link=, =search_job_posts=, =get_job_posts=, =update_job_post= |
| Applications (4) | =create_job_application=, =get_job_applications=, =get_applications_for_job_post=, =update_job_application= |
| Career data (1) | =get_career_data= |
| Resumes (1) | =get_resumes= |
| Scrapes (3) | =create_scrape=, =get_scrapes=, =update_scrape= |
| Scores (2) | =score_job_post=, =get_scores= |
All tools live in =ai/lib/api_tools.py=. Each takes =api: ApiClient= as first arg plus typed parameters.
Returns: JSON string (=APIResponse= format: ={success, data, error, status_code}=).
Tool responses include =_frontend_url= on each resource — the agent MUST use these in markdown links.

** Toolset scopes (for non-chat agents)
| Scope | Tools | Use case |
|-------|-------|----------|
| =all= | 27 | Chat (full access) |
| =career_caddy= | 16 | General CRUD + career data + resumes |
| =job_discovery= | 7 | Email pipeline: find/create companies + job posts |
| =scoring= | 4 | Scoring: job posts + career data + scores |
| =application_tracking= | 6 | Application CRUD + job post updates |
| =scrape_management= | 3 | Scrape CRUD |

** History management
- Frontend sends last 20 messages (excluding current turn) as =history= array
- Chat server converts to pydantic-ai =ModelRequest=/=ModelResponse= objects
- =truncate_message_history=: drops old messages when history exceeds ~20k tokens (estimated at 3 chars/token)
- =sanitize_orphaned_tool_calls=: removes tool call/return pairs where not all parallel calls were answered (handles MCP crashes and partial failures)
- Tool response cap: 300k chars max per individual tool response (prevents context blowout)

** Frontend chat features
*** Sidebar vs full-page
- Sidebar: =<Chat::Sidebar>= wraps =<Chat::Panel>=, appears as CSS grid area =course-chat= (24rem wide)
- Full-page: =/caddy= route renders =<Chat::Panel>= in =<RouteLayout>= main area
- Both share the singleton =chat= service — messages persist across route transitions
- Sidebar and nav sidebar are mutually exclusive (opening one closes the other)

*** Markdown rendering
- Assistant messages render through =<MarkdownToHtml>= (ember-cli-showdown)
- User messages render as plain =whitespace-pre-wrap= text
- CSS class =.chat-markdown= provides compact styling (tighter than =.markdown-content=)

*** SPA link navigation
=chat/message.js= intercepts clicks on =<a href>= inside assistant messages.
Relative paths route through =router.transitionTo()= — no page reload.
External links (=http=, =mailto:=) pass through normally.

*** Programmatic navigation
Two mechanisms:
1. AI includes =<!-- navigate:/path -->= in text. Frontend regex strips it and calls =router.transitionTo()=.
2. Server emits ={type: "navigate", url: "/path"}= SSE event. Frontend handles it in the SSE reader.
Both navigate only on clear user intent ("take me to", "show me").

*** Elicitation buttons (Phase 1)
AI emits a =```json= code block at end of response: ={elicitation: true, actions: [{label, message}]}=.
=chat/message.js= detects via regex, renders accent-colored buttons below markdown.
Clicking sends =action.message= as a new user chat message. Max 3 actions.

*** Page context
=main-application.js= sets =chat.currentPage = {route, url, params}= on every route change.
Sent in the chat POST body. Chat server injects "Current Page" section into system prompt.
Enables the AI to understand "this job post" or check data on the current page.

*** Auth boundary messages
- 401 (not logged in): friendly markdown message with [login](/login) link
- 403 (guest account): markdown message suggesting signup with [login](/login) link
- Both use SPA link navigation (no page reload)

*** Streaming UX
- "Thinking..." pulse animation while =isStreaming= is true
- Text accumulates chunk by chunk, message updates in place
- Auto-scroll to bottom after each chunk

** Usage tracking
- =report_usage()= fires async after each chat response (fire-and-forget)
- POST to =/api/v1/ai-usages/= with: agent_name="career_caddy_chat", model_name, trigger="chat", token counts
- =AiUsage= model: user FK, agent_name, model_name, trigger, request/response/total tokens, estimated_cost_usd
- =AiUsageViewSet=: non-staff sees own records only, staff can filter by =?user_id==
- Summary endpoint: =/ai-usages/summary/?period=daily&group_by=agent_name&days=30=
- Frontend: Settings > AI Spend page with staff-only user dropdown

** Security invariants
- =chat_server.py= MUST NOT import =email_server=, =browser_server=, or =lib/browser/*=
- No =CC_API_TOKEN= env var on the chat service — auth comes from the calling proxy
- No =secrets.yml= or mail directory access
- =api_tools.py= imports only =httpx=, =json=, =pydantic= — zero knowledge of browser/email automation
- =CareerCaddyDeps= holds only =api_token= + =base_url= — no credential paths

** Docker deployment
- Chat server: separate container, port 8031, =CHAT_SERVICE_URL= env var in API container
- =CC_API_BASE_URL= in chat container points to the API (e.g. =http://api:8000=)
- =ALLOWED_HOSTS= in API settings must include the Docker hostname (=api=) for tool calls to work
- Chat container does not need =INSTALL_CAMOUFOX= or browser dependencies

* AI Chat Improvements
:PROPERTIES:
:CREATED:  [2026-04-13]
:END:

** Implementation strategy
8 items from todo.org grouped into clusters. Full plan at =~/.claude/plans/generic-gathering-quill.md=.
- Cluster A (page context + navigation + onboarding): items 3, 4, 6
- Cluster B (AI data bugs): items 1, 2
- Cluster C (infrastructure): items 5, 7, 8
Priority order: 8 → 2 → 1 → 3 → 4 → 6 → 5; item 7 (websockets) deferred.

** Item 8: AI usage scoping — DONE (frontend only)
Backend =AiUsageViewSet= already scopes non-staff to own records and accepts =?user_id== for staff.
Changes: =settings/ai-spend.js= controller gets =@tracked selectedUserId=, =@tracked users[]=,
=loadUsers()= fetches user list for staff, =setUser()= re-queries. Template adds user dropdown
visible only to =isStaff= users. "All users" default.

** Item 2: Resume counting — DONE (AI layer)
Root cause: =TOOL_REGISTRY= had zero resume tools. AI used =get_career_data()= which only surfaces favorites.
Added =get_resumes()= to =api_tools.py= (pattern matches =get_job_posts=). Registered in
=toolsets.py= under =all= and =career_caddy= scopes. System prompt note: "Use get_resumes, not career data."

** Item 1: Company inference — DONE (AI layer)
Blocklist in =create_job_post_with_company_check=: rejects ={unknown, n/a, na, none, tbd, not specified, ""}=
(case-insensitive). Returns error with inference instructions. System prompt: "NEVER use placeholder
company names. Infer from recruiter, email domain, or URL domain. Ask user if stuck."

** Item 3: Page context — FIXED (all layers)
Frontend =chat.js= tracks =@tracked currentPage= updated on every route change in =main-application.js=.
Payload includes =page_context: {route, url, params}=. Django proxy forwards. Chat server's
=_build_agent()= accepts =page_context= and appends a "Current Page" section to the system prompt.

#+NOTE: [2026-04-13] Three issues found and fixed:
1. *Model hallucinated page names* — data was arriving correctly (confirmed via diagnostic logging)
   but gpt-4o-mini ignored the route name in the system prompt. Fix: tightened prompt wording
   to say "reply with the URL path, do NOT rephrase or invent a page name."
2. *Stale context on follow-up messages* — agent was built with page context from message 1;
   message 2 after navigation still showed the old page. Fix: inject =[Current page: /url]=
   prefix into each user message so page changes are visible in the conversation history.
3. *Eager init* — added =currentPage= initialization in =main-application.js= constructor
   so page context is set before first =routeDidChange= fires.

** Item 3b: Page-aware data access — DONE (AI layer)
Route-to-tool mapping table added to chat system prompt. Agent now maps URL paths to tool calls:
=/resumes= → =get_resumes()=, =/job-posts/42= → =get_job_posts(id=42)=, etc.
Resource ID extracted from URL via regex in =_build_agent()= and injected into the Current Page
section so the model doesn't need to parse it. Enables "what's on this page?" / "tell me about this"
queries to fetch real data from the API matching the user's current screen.

** Item 4: Navigation — DONE (frontend + AI)
Two mechanisms: (1) AI includes =<!-- navigate:/path -->= in text — frontend regex =NAVIGATE_RE=
detects, strips from displayed text, and calls =router.transitionTo()=. (2) Chat server can emit
={type: "navigate", url: "/path"}= SSE event — frontend handles alongside =text/done/error= events.
System prompt "Navigation" section instructs when to use it.

** Item 6: Onboarding — DONE (system prompt)
New "Onboarding Help" section in system prompt. On greetings, AI checks current page context,
queries the relevant tool, and suggests creating items if the list is empty. Depends on items 2+3.

** Item 5: Elicitation — DONE Phase 1 (frontend + AI)
AI emits a =```json= code block with ={elicitation: true, actions: [{label, message}]}= at end
of response. Frontend =chat/message.js= detects via regex, extracts actions, renders buttons
below the markdown content. Clicking sends the action's =message= as a new chat message via
=chat.sendMessage()=. Max 3 actions per elicitation. No new backend model.
Phase 2 (dedicated =ChatAction= model with persistence) deferred until Phase 1 is validated.

** Item 7: WebSockets — DEFERRED
SSE handles streaming + elicitation POST-back. WebSockets would need django-channels + ASGI +
Redis + Docker changes. Marginal benefit. Revisit only if bidirectional becomes a hard requirement.

* Admin AI Model Configuration
:PROPERTIES:
:CREATED:  [2026-04-13]
:END:
Plan for an admin UI to change which AI model each agent uses at runtime.
[[file:todo.org::*admin interface for AI model selection][↑ todo.org]]

** Motivation
=run_email_web.py= in the AI submodule lets you pick models on the fly via pydantic-ai's =agent.to_web(models=[...])= dropdown. We want the same flexibility inside Career Caddy's admin interface — no service restarts, no =.env= editing.

** Current state
All agents resolve models through =agent_factory.py:get_model(role)=:
1. Role-specific env var (e.g. =CHAT_MODEL=, =CADDY_MODEL=)
2. =CADDY_DEFAULT_MODEL= env var
3. Hardcoded ="openai:gpt-4o-mini"=

| Role              | Env Var                  | Purpose                               |
|-------------------+--------------------------+---------------------------------------|
| =chat=            | =CHAT_MODEL=             | In-app AI chat sidebar                |
| =caddy=           | =CADDY_MODEL=            | Creates/manages job posts, apps       |
| =email_classifier=| =EMAIL_CLASSIFIER_MODEL= | Classifies emails as job postings     |
| =job_extractor=   | =JOB_EXTRACTOR_MODEL=    | Extracts structured data from job text|
| =pipeline=        | =PIPELINE_MODEL=         | Searches emails, extracts URLs        |
| =browser_scraper= | =PIPELINE_MODEL=         | Scrapes web pages for job data        |

** Architecture options evaluated

*** Option A — DB-stored configs with cached API fetch ⟵ RECOMMENDED
Store model overrides in Django DB. AI service fetches from a lightweight API endpoint with 30s TTL cache.

Fallback chain: DB config → role env var → =CADDY_DEFAULT_MODEL= → hardcoded default.

*Pros:*
- Instant model switching from admin UI — no restart
- Configs persist across deployments (DB-backed)
- Graceful degradation — API unreachable → cache → env vars → default
- Consistent with existing patterns (DB + API)
- Audit trail via =updated_by= field

*Cons:*
- Adds API dependency to =get_model()= hot path (mitigated by 30s TTL cache, 5s timeout)
- New DB table for what's currently a simple env var
- 0–30s propagation delay after changing model in UI
- Stale cache if API goes down (acceptable — was valid when set)

*** Option B — Shared config file (Docker volume mount)
Write YAML/JSON from Django, mount into AI container.

*Pros:* No network call, no cache.
*Cons:* Requires shared volume, doesn't work in prod (separate hosts), filesystem race conditions, no audit trail.

*** Option C — Docker API env var hot-reload
Use Docker API to update env vars and trigger graceful restart.

*Pros:* Zero code changes to =agent_factory.py=.
*Cons:* Docker socket access = security risk, causes downtime, doesn't work outside Docker.

*** Option D — Redis pub-sub
Store in Redis, AI subscribes to changes.

*Pros:* Near-instant propagation.
*Cons:* Adds Redis as new dependency, overkill for 6 values that change rarely.

** Cost considerations
| Model                                | Input / Output per 1M tokens | vs. gpt-4o-mini |
|--------------------------------------+------------------------------+-----------------|
| =openai:gpt-4o-mini=                | $0.15 / $0.60                | 1x (baseline)   |
| =anthropic:claude-haiku-4-5=        | $0.80 / $4.00                | ~5x             |
| =openai:gpt-4o=                     | $2.50 / $10.00               | ~17x            |
| =anthropic:claude-sonnet-4=         | $3.00 / $15.00               | ~20x            |
| =ollama:*=                          | Free (local compute)         | 0x              |

No guardrails in v1 — admin is trusted. AI Spend page already tracks per-model usage.

** Implementation summary
Full plan at =~/.claude/plans/linked-finding-crayon.md=.

*** New files (6)
- =api/job_hunting/models/agent_model_config.py= — Django model
- =ai/lib/model_config_cache.py= — 30s TTL cache
- =frontend/app/models/agent-model-config.js= — Ember Data model
- =frontend/app/routes/admin/ai-models.js= — route
- =frontend/app/controllers/admin/ai-models.js= — controller
- =frontend/app/templates/admin/ai-models.hbs= — template

*** Modified files (11)
- =api/= — models/__init__.py, serializers.py, views.py, urls.py
- =ai/= — agents/agent_factory.py (=get_model()= gains DB check before env vars)
- =frontend/= — router.js + 7 admin templates (add "AI Models" subnav link)

*** Key design decisions
- =<datalist>= input: curated suggestions + free-text entry (not a locked dropdown)
- =@action(detail=False, url_path="active")= returns plain JSON for machine consumption
- Cache uses module-level globals (AI service is single-process)
- Chat server needs =CC_API_TOKEN= added to its Docker env

** Risks
- *Invalid model string*: pydantic-ai fails at runtime → chat error. v1 accepts it; future "Test" button could validate.
- *Cache staleness*: if API goes down, stale config persists. Acceptable — was valid when set, env-var fallback still works.
- *Ollama models*: entered as strings, but =agent_factory.py= currently constructs =ConcreteOllamaModel= objects for Ollama. Free-text =ollama:model= may need factory-level parsing. Workaround: use =openai:= prefix with Ollama's OpenAI-compatible endpoint.

** Out of scope
- Per-user model selection
- Model string validation / test button
- Cost budgets or alerts
- A/B testing or gradual rollout
- System prompt editing from admin UI
- Ollama model discovery (query Ollama API)

* Hold Poller, Screenshots, Multi-Model Pipeline
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:
[[file:todo.org::*MCP exposure at mcp.careercaddy.online][↑ todo.org (MCP exposure)]]
[[file:todo.org::*Design plans for surfacing browser screenshots][↑ todo.org (screenshots)]]

Full plan: =~/.claude/plans/fluffy-discovering-jellyfish.md=

** Motivation
The browser scraper agent is a pass-through that wastes LLM tokens — the LLM just calls =scrape_page(url)= and echoes the result. By calling =scrape_page()= directly as a Python function and only using LLMs for the intelligent steps (job extraction via Sonnet, CRUD via gpt-4o-mini), we save ~56% per scrape.

Browser scraping on credentialed sites fails silently in prod Docker (no sessions/cookies). The hold-scrape pattern lets admin queue URLs from the prod UI, then a local poller processes them with a headed browser and saved sessions.

** Architecture

*** Multi-model pipeline (cost-optimized)
| Step                  | Model              | Cost     |
|-----------------------+--------------------+----------|
| =scrape_page()=       | None (direct call) | $0.00    |
| =extract_job_from_content()= | Claude Sonnet      | ~$0.10   |
| =add_job_post()=      | gpt-4o-mini        | ~$0.002  |
| *Total per scrape*    |                    | *~$0.10* |

*** Hold poller — scrape-only worker

*What it is:* A local process (not Docker) that polls the Career Caddy API for
scrapes with =status=hold=, runs Camoufox to fetch the page, and PATCHes the
content back. The API handles everything downstream (extraction, job post
creation, scrape profile updates).

*Why it exists:* Browser scraping is too heavy for the VPS. The poller runs on
a desktop or Pi, keeping the VPS as a thin API + frontend layer.

*Running it:*
| Method | Command |
|--------+-----------------------------|
| Standalone (prod) | =cd ai && cp .env.prod.example .env && uv run caddy-poller= |
| Standalone (local dev) | =cd ai && CC_API_BASE_URL=http://localhost:8000 CC_API_TOKEN=xxx uv run caddy-poller= |
| With dev stack | =make up-full= (Docker stack + poller in background) |

*Prerequisites:*
- =uv sync= + =uv run python -m camoufox fetch= (one-time)
- =CC_API_BASE_URL= and =CC_API_TOKEN= env vars
- No LLM keys needed on the worker — all LLM work happens API-side

*Backoff:* The poller uses exponential backoff: 2, 4, 8, 16, 32, 64, 128, 256
seconds. Resets to 2s when work is found. Stops after the full sequence with no
work (~510s total). This means you run it, it processes any hold scrapes, backs
off, and exits. Run it again when you queue more holds.

*Flow:*
#+BEGIN_SRC
caddy-poller starts
  poll: GET /api/v1/scrapes/?filter[status]=hold
  found work?
    yes → for each hold scrape:
            PATCH status → running (note: "Poller picked up")
            scrape_page(url) — browser engine, no LLM
              login wall? → status=failed (note: login wall message)
              success? →
                POST screenshot to /scrapes/{id}/screenshots/
                PATCH job_content + status=completed (note: "Content delivered (N chars)")
                → API fires _maybe_trigger_extraction():
                  parse_scrape() → LLM extraction (gpt-4o-mini)
                  _update_scrape_profile() → domain stats
                  _generate_profile_hints() → LLM hints (first time only)
            reset backoff to 2s
    no  → backoff: 2 → 4 → 8 → 16 → 32 → 64 → 128 → 256 → stop
#+END_SRC

*Env vars:*
| Var | Default | Purpose |
|-----+---------+---------|
| =CC_API_BASE_URL= | (required) | API base URL |
| =CC_API_TOKEN= | (required) | API key (=jh_*= or =Api-Key=) |
| =BROWSER_HEADLESS= | =true= | Set =false= for VNC debugging (pibu has VNC) |
| =HOLD_POLL_INTERVAL= | =30= | Initial poll interval (overridden by backoff) |

*Platform notes:*
- Needs 64-bit OS (=aarch64= or =x86_64=) — Playwright/Camoufox have no =armv7l= wheels
- Pibu (32-bit Ubuntu) won't work; desktop is the current target
- Pi 4+ with 64-bit OS would work

*** ScrapeStatus (polymorphic Status pattern)
Mirrors =JobApplicationStatus=. Uses =Status= table with =status_type="scrape"=.
Statuses: hold, pending, running, authenticating, login_failed, scraping, extracting, completed, failed.
Each transition logged with timestamp and note (error details, content length, screenshot path).

*** Screenshot serving [updated 2026-04-16]
- Poller uploads via =POST /api/v1/scrapes/{id}/screenshots/= (multipart form-data)
- Stored in =screenshots/{scrape_id}/= folders (folder-per-scrape, not flat prefix naming)
- =ScreenshotStore= abstraction in API — =list_for_scrape()=, =read()=, =save()=. Local disk now, S3 later.
- Staff-only endpoints: =GET /api/v1/scrapes/{id}/screenshots/= (list filenames), =GET .../screenshots/{filename}= (serve PNG)
- DRF format suffix routing strips =.png= — manual =re_path= in =urls.py= bypasses this for file serving
- Frontend: staff sees filename list on scrape show, click to reveal image (fetched with JWT via blob URL)
- No shared Docker volume needed — poller uploads through the API regardless of where it runs

*** Browser headless mode
=BROWSER_HEADLESS=false= env var makes Camoufox visible. Defaults to =true= (no change to prod).
The =screenshot= MCP tool was listed in the docstring but never implemented — now implemented.
Terminology: =snapshot= = text extraction (=page.inner_text=), =screenshot= = PNG image (=page.screenshot=).

*** Chat-initiated scrapes
:PROPERTIES:
:CREATED:  [2026-04-14]
:UPDATED:  [2026-04-16]
:END:
Chat server =create_scrape()= now passes =status="hold"= so the hold-poller picks them up.
Previously it omitted status (API defaulted to ="pending"= and dispatched browser-mcp directly),
but browser-mcp is no longer running on the VPS. Both chat and public MCP server use hold.

System prompt updated: after creating a scrape, the AI offers elicitation buttons
("View scrape" → navigates user to =/scrapes/{id}=).

#+NOTE: [2026-04-16] New clones will have no poller running, so chat-created scrapes sit
in hold. This needs documentation in README and/or .env defaults.

*** ScrapeStatus logging gaps
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:
=ScrapeViewSet.create=, =.redo=, and hold creation never called =_log_scrape_status()=.
Only the background scraper thread logged statuses (=running=, =scraping=, etc.).
Result: scrape-status timeline was empty until the background thread ran.
Fix: all three entry points now log the initial status (=pending=, =hold=, or =pending + "Redo requested"=).

** .env model config (defers DB-based admin UI)
Per-agent model overrides already work via env vars in =agent_factory.py:get_model()=.
=BROWSER_SCRAPER_MODEL= gets its own env var (was sharing =PIPELINE_MODEL=).
The DB-based admin model config (Option A in notes above) stays PENDING_APPROVAL.

* Domain Scrape Profiles — learning loop for scrape cost reduction
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:
[[file:todo.org::*Domain scrape profiles][↑ todo.org]]

** Motivation
The job extractor LLM call (~$0.025/scrape with Haiku) is the main per-scrape cost. For domains scraped frequently (LinkedIn, Indeed, Glassdoor), the LLM re-discovers the same page structure every time. A domain-level profile that captures what worked before could eliminate or reduce the LLM cost on repeat scrapes.

** Model: =ScrapeProfile= (per hostname)
| Field                | Type     | Purpose                                              |
|----------------------+----------+------------------------------------------------------|
| =hostname=           | str (PK) | Normalized domain (e.g. =linkedin.com=)              |
| =requires_auth=      | bool     | Learned from =login_wall_detected= failures          |
| =avg_content_length= | int      | Baseline to detect anomalies (too short = blocked)   |
| =success_rate=       | float    | Track reliability over time                          |
| =css_selectors=      | json     | Selectors that work for this domain's job pages      |
| =extraction_hints=   | text     | Free-text LLM context: "salary in EUR", etc.         |
| =page_structure=     | text     | LLM-generated description of the page layout         |
| =last_success_at=    | datetime | Stale profile detection                              |
| =scrape_count=       | int      | Usage tracking                                       |
| =created_by=         | FK       | Multi-tenancy                                        |

** How the learning loop works
1. First scrape of =linkedin.com= → no profile → full LLM extraction ($0.025)
2. Extraction succeeds → auto-populate profile from the result
   - LLM writes =extraction_hints=: "title in =.top-card-layout__title=, company in =.topcard__org-name-link=, salary format USD annual"
   - =avg_content_length= set from this scrape
   - =requires_auth= set from ScrapeStatus history
3. Next =linkedin.com= scrape → profile hints injected into extractor prompt → cheaper/faster
4. If extraction fails or quality degrades → fall back to full LLM, flag profile as stale
5. Staff can review/edit profiles in admin UI

** Tiered scrape cost structure
| Tier | When                          | Model          | Cost/scrape |
|------+-------------------------------+----------------+-------------|
|    0 | Known domain + CSS selectors  | None (determ.) | $0.00       |
|    1 | Known domain + hints          | gpt-4o-mini    | ~$0.005     |
|    2 | Unknown domain                | Haiku          | ~$0.025     |
|    3 | Complex / high-value page     | Sonnet         | ~$0.10      |

Tier selection can be automatic (profile exists? use it) or manual (admin chooses tier per scrape).

** How extraction_hints get written
After a successful extraction, ask the LLM one follow-up question:
#+begin_src
"You just extracted a job posting from {hostname}. Describe the page structure
in 2-3 sentences so a cheaper model can extract from similar pages next time.
Include: where the title, company, salary, and description appear on the page."
#+end_src
This is a one-time ~$0.01 cost that pays off over many future scrapes.

** Integration points
- =scrape_page()= → check =ScrapeProfile.requires_auth= before scraping, skip if no session
- =extract_job_from_content()= → inject =extraction_hints= + =page_structure= into system prompt
- =hold_poller.py= → after successful extraction, update or create profile
- =GenericParser= (API-side) → same hint injection for server-side extraction
- Frontend admin → CRUD for =ScrapeProfile= records (review, edit hints, toggle tiers)

** Deterministic extraction (Tier 0)
For well-known domains, CSS selectors stored in the profile can extract data without any LLM:
#+begin_src python
profile = ScrapeProfile.objects.get(hostname="linkedin.com")
if profile.css_selectors:
    title = page.query_selector(profile.css_selectors["title"]).inner_text()
    company = page.query_selector(profile.css_selectors["company"]).inner_text()
    # ... build JobPostData directly
#+end_src
Falls back to LLM if any selector fails. This brings repeat-domain cost to $0.00.

** Frontend: staff admin page for scrape profiles
Lives under =/admin/scrape-profiles= as a new subnav item alongside API Keys, Users, Waitlist, Invitations.

*** Routes
- =admin.scrape-profiles.index= → table of all profiles
- =admin.scrape-profiles.show= → detail view + edit form

*** Router
#+begin_src js
// in admin route group:
this.route('scrape-profiles', function () {
  this.route('show', { path: '/:scrape_profile_id' });
});
#+end_src

*** Index page (table)
| Column           | Source                       |
|------------------+------------------------------|
| Hostname         | =hostname= (link to show)    |
| Auth Required    | =requires_auth= (badge)     |
| Success Rate     | =success_rate= (% + color)  |
| Scrape Count     | =scrape_count=               |
| Last Success     | =last_success_at= (relative)|
| Tier             | =preferred_tier= (pill)     |

Sorted by =scrape_count= descending (most-scraped domains first).
No "create" button — profiles are auto-created by the learning loop. Staff can only view and edit.

*** Show/edit page
Split into two panels:

**** Stats panel (read-only)
- Hostname, requires_auth, success_rate, scrape_count, last_success_at, avg_content_length
- Link to scrapes list filtered by this hostname: =/scrapes?filter[hostname]=linkedin.com=

**** Configuration panel (editable by staff)
- =extraction_hints= — textarea, LLM-generated but staff-editable. Staff can refine hints the LLM wrote, add domain-specific notes ("this site puts salary in the sidebar, not the description").
- =page_structure= — textarea, LLM-generated description. Staff can correct.
- =css_selectors= — JSON editor or key-value pairs (title, company, salary, location, description). For Tier 0 deterministic extraction.
- =preferred_tier= — dropdown: Auto, Tier 0, Tier 1, Tier 2, Tier 3. "Auto" lets the system decide based on profile completeness.
- =enabled= — toggle. Disable a profile if it's producing bad results without deleting it.

Save → =PATCH /api/v1/scrape-profiles/{id}/=

*** Subnav update
Add to every admin template's =<:subnav>= block:
#+begin_src handlebars
<LinkTo @route="admin.scrape-profiles.index" class="nav-link">Scrape Profiles</LinkTo>
#+end_src

*** API endpoint
=GET /api/v1/scrape-profiles/= — staff-only list (=IsAdminUser= permission)
=GET /api/v1/scrape-profiles/{id}/= — staff-only detail
=PATCH /api/v1/scrape-profiles/{id}/= — staff-only update (no create/delete; auto-managed)

*** Ember Data model
#+begin_src js
// app/models/scrape-profile.js
export default class ScrapeProfileModel extends Model {
  @attr('string') hostname;
  @attr('boolean') requiresAuth;
  @attr('number') avgContentLength;
  @attr('number') successRate;
  @attr() cssSelectors;           // raw JSON
  @attr('string') extractionHints;
  @attr('string') pageStructure;
  @attr('date') lastSuccessAt;
  @attr('number') scrapeCount;
  @attr('string') preferredTier;
  @attr('boolean') enabled;
}
#+end_src

** Model updates (from frontend spec)
Add to =ScrapeProfile= model:
| =preferred_tier= | str    | "auto", "0", "1", "2", "3" — staff override for tier selection |
| =enabled=        | bool   | Disable without deleting; system skips disabled profiles       |

** Risks
- Page structure changes (LinkedIn redesigns) → stale profiles give wrong data. Mitigated by =last_success_at= staleness check and fallback to full LLM.
- Over-fitting hints to one page layout → different job post types on same domain may differ. Mitigated by tracking =success_rate= and invalidating below threshold.
- CSS selectors break silently → need a "confidence score" on selector-based extraction (e.g., all expected fields present? content length reasonable?).

** Implementation log
*** [2026-04-14] ScrapeProfile model created (migration 0040)
=api/job_hunting/models/scrape_profile.py= — fields match spec above plus =preferred_tier= and =enabled=.
Uses =GetMixin=, =auto_now_add= on =created_at=, =auto_now= on =updated_at=.
=hostname= is unique + indexed. =created_by= FK for multi-tenancy.
*** [2026-04-14] Hold poller E2E test — bugs found and fixed
Three bugs discovered during E2E testing of hold poller + screenshots:
1. =hold_poller.py:149= — Auth header used =Api-Key= format but API expects =Bearer jh_...=. Removed override.
2. =browser_server.py:685= — =logfire.info("msg %s", arg)= crashed (logfire doesn't support printf-style). Changed to f-string.
3. =career_caddy_agent.py:18= — =logfire.configure()= at module level prompts for interactive setup when =LOGFIRE_TOKEN= absent. Guarded with env check.
Core flow validated: poll → find hold → scrape with Camoufox → screenshot capture + rename → content save → status transitions.
Extraction step deferred (needs =OPENAI_API_KEY= in env).
*** [2026-04-14] Staff-only API endpoints
=ScrapeProfileSerializer= + =ScrapeProfileViewSet= (list, retrieve, partial_update).
- =permission_classes = [IsAuthenticated, IsAdminUser]=
- =filter[hostname]= query param on list
- Editable fields: =extraction_hints=, =page_structure=, =css_selectors=, =preferred_tier=, =enabled=
- Registered at =/api/v1/scrape-profiles/=
*** [2026-04-14] Frontend admin pages
- Ember model: =scrape-profile.js= with computed =tierLabel=, =successRatePercent=, =successRateClass=
- Routes: =admin.scrape-profiles.index= (list table) + =admin.scrape-profiles.show= (stats + edit form)
- Controller uses =@tracked= intermediary fields for inputs
- CSS selectors field validates JSON before save
- Subnav link added to all 9 admin templates
*** [2026-04-14] AI integration — auto-populate, hints, Tier 0
- =_update_scrape_profile()= auto-creates/updates =ScrapeProfile= after every successful parse.
  Rolling average for =avg_content_length=, cumulative =success_rate=.
- =_log_scrape_status()= sets =requires_auth=True= on login_failed via =ScrapeProfile.update_or_create=.
- =_get_profile_hints()= injects =extraction_hints= + =page_structure= into the extractor prompt.
- =_generate_profile_hints()= makes a gpt-4o-mini call with =ProfileHints= Pydantic model (structured output).
  Records =AiUsage= with =trigger="scrape_profile"=, =agent_name="scrape_profile_hints"=.
- =_try_tier0_extraction()= attempts deterministic CSS extraction via BeautifulSoup before LLM.
  Requires =css_selectors= JSON + =preferred_tier= in ("auto", "0"). Falls back to LLM if title/company missing.
- =_get_genesis_user()= fallback: AI costs always attributed (first staff user if no user on scrape).

* Scrape → Parse → JobPost flow (documented for refactor)
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:

** Current entry points (3 paths, should be 1)

*** Path 1: MCP dispatch (automatic — scrape created via API)
#+begin_src
ScrapeViewSet.create (views.py:4358)
  → Scraper(url, scrape_id).dispatch() (scraper.py:30)
    → _dispatch_mcp._send() runs in daemon thread
      → MCP client calls browser-mcp scrape_page
      → Stores job_content on Scrape, sets status="completed"
      → _maybe_caddy_extract(scrape) (scraper.py:140)
        → extract_job_from_scrape(scrape) (generic_parser.py:189)
          → Guard: job_content AND status=="completed" AND no job_post_id
          → Spawns ANOTHER daemon thread
            → GenericParser().parse(scrape, user=scrape.created_by)
              → analyze_with_ai() — LLM extracts ParsedJobData
              → process_evaluation() — creates Company + JobPost + links scrape
#+end_src
Problems:
- Two nested daemon threads (dispatch thread spawns extract thread)
- user= was None until just fixed — created orphan job posts (404)
- No status logging on the extract step (added _log_scrape_status now)
- No error surfacing to frontend — exceptions swallowed silently

*** Path 2: Manual parse button (frontend → POST scrape/{id}/parse/)
#+begin_src
ScrapeViewSet.parse (views.py:4494)
  → Sets status="extracting" via _log_scrape_status
  → Spawns daemon thread
    → GenericParser().parse(scrape, user=request.user)
    → On success: _log_scrape_status("completed")
    → On failure: _log_scrape_status("failed")
#+end_src
This is the cleanest path. Returns 200 immediately, frontend polls.

*** Path 3: Hold poller (local script — ai/scripts/hold_poller.py)
#+begin_src
hold_poller.poll_once()
  → GET /api/v1/scrapes/?filter[status]=hold
  → For each: scrape_page(url) directly (no LLM)
  → PATCH scrape with job_content + status
  → extract_job_from_content() — AI layer LLM
  → add_job_post() — AI layer API call
#+end_src
This path bypasses GenericParser entirely — uses the AI layer's own agents.

** What's wrong

1. *Three different parse paths* with different user handling, error handling, and status logging
2. *Two nested threads* in Path 1 — dispatch thread spawns extract thread. Hard to reason about.
3. *GenericParser.get_agent()* (line 72) hardcodes =gpt-4o= / =qwen3-coder= — doesn't use =agent_factory.get_model()=. Disconnected from the env var model config.
4. *No status logging in Path 1* extract step — goes straight from "completed" (content saved) to either silently creating a job post or silently failing.
5. *extract_job_from_scrape guard* (line 195) checks =status=="completed"= — but Path 2 sets status to "extracting" before calling parse. So if Path 2 is used, the guard in =extract_job_from_scrape= would reject it. They're separate flows that happen to call the same parser.
6. *Company created without created_by* — =Company.objects.get_or_create(name=...)= has no user scoping. Multi-tenancy gap.

** DONE Refactor completed
CLOSED: [2026-04-14]
Renamed =GenericParser= → =JobPostExtractor= (file: =job_post_extractor.py=).
Single =parse_scrape(scrape_id, user_id, sync)= entry point replaces both =extract_job_from_scrape()= and inline thread in =ScrapeViewSet.parse=.
- Env-var model selection: =JOB_PARSER_MODEL= → =CADDY_DEFAULT_MODEL= → =gpt-4o= default. Supports =ollama:model-name= prefix.
- =sync=True= eliminates nested threads (dispatch already backgrounded).
- Status transitions: =extracting → completed/failed= with notes — consistent across all paths.
- 13 new tests in =test_job_post_extractor.py=. All tests pass.

*** Company is a shared resource — no =created_by= :DECISION:
Company must NOT have a =created_by= field. Companies are shared across all users in a multi-tenant setup — "Google" is the same Company regardless of who scraped it first. Adding =created_by= would imply ownership where there is none, and would break the pattern where multiple users assign their own job posts to the same company. No migrations needed. Decided [2026-04-14].

** Polling refactor: poll ScrapeStatus, not Scrape [TODO]
Currently the frontend polls the scrape record AND the scrape-statuses endpoint — two requests per tick. The =ScrapeStatus= join table is the source of truth; =Scrape.status= (CharField) is a denormalized cache updated by =_log_scrape_status()=.

The correct approach: poll =GET /api/v1/scrapes/{id}/scrape-statuses/= only. Derive current state from the latest =ScrapeStatus= record. On terminal state, fetch the full scrape with =?include=company,job-post= once.

This requires:
- A custom poll loop in the controller (the =poller= service expects an Ember Data record with =.reload()=)
- Or extend the poller service to accept a custom fetch function instead of relying on =record.reload()=
- The scrape-statuses endpoint already returns =included= status records with the status label

Current state works (two requests per tick) but is wasteful. Consolidate when the poller service is refactored.

* Builder-form style conventions                                    :IMPORTANT:
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:
Reference for agents working with =builder-form= in =frontend/app/styles/app.css=.

** Layout classes
| Class | Behaviour | Use for |
|-------+-----------+---------|
| =builder-form= | 2-col grid: =max-content 1fr= | Default form layout (label + input per row) |
| =builder-form.two-col= | Equal 2-col grid, fields side-by-side | Settings profile, questions form |
| =.field= | Direct child: =display:contents= (dissolves into grid) | Standard label+input pair |
| =.field-full= | =flex-direction:column=, spans both cols. *All* child =input/textarea= get =width:100%= | Full-width fields (address, description, question content) |
| =.field-row= | =display:flex=, spans both cols. Does *not* force child input width | Inline groups where you control widths with Tailwind |
| =.form-actions= | Button row at bottom, spans both cols | Save / Cancel / Delete |

** Key gotcha: =.field-full= forces =width:100%= on all descendant inputs
If you put a grid or flex row of multiple inputs inside =.field-full=, each input expands to 100% and stacks vertically. Use =.field-row= instead when you need multiple inputs on one line (e.g. the links editor on settings/profile).

** Inline styles
Never use inline =style="..."= attributes. Tailwind utilities are the standard. If a Tailwind class is being overridden by =builder-form= CSS, choose the correct form layout class (=field-row= vs =field-full=) rather than fighting it with inline styles.

** Read-only views
Match the edit form's column layout. Use =grid grid-cols-2 gap-x-6 gap-y-4= on the =<dl>=. Full-width items (address, links) get =col-span-2=.

* Resume serializer — JSON:API relationship linkage + sideloading
:PROPERTIES:
:CREATED:  [2026-04-14]
:END:
RESOLVED [2026-04-14]

** What was wrong
=ResumeSerializer.to_resource= embedded sub-resources (summaries, certifications,
educations, experiences, skills, projects) as plain JSON arrays in =attributes=.
It declared them as =relationships= with =links= but *no =data= linkage*.

Ember Data ignored the embedded attributes and tried to fetch via =related= URLs
(e.g. =/api/v1/resumes/30/certifications=) — which don't exist as nested endpoints.
Result: 404s and missing data (summaries carousel, certifications display).

** What was fixed

*** ResumeSerializer (=api/job_hunting/api/serializers.py=)
Removed ~130 lines of manual attribute embedding. Added proper JSON:API =data= linkage
arrays using the existing =get_related()= method. Pattern matches =DjangoUserSerializer=.
The =summary= convenience attribute (active summary text string) is kept.

*** ExperienceSerializer (same file)
Added =data= linkage for =descriptions= relationship. Without this, Ember Data couldn't
match description records in =included= to their parent experience.

*** ResumeViewSet (=api/job_hunting/api/views.py=)
Added =_default_includes= class attribute. Both =list()= and =retrieve()= auto-include
all sub-resources when no =?include== param is specified. No extra frontend fetches needed.

*** _build_included ownership filter (=views.py=)
Resources with =user_id=None= were silently dropped by the ownership filter
(=None != user_id= is =True=). Fixed: skip the filter when =user_id is None= — if
reached through a relationship on an already-authorized parent, they're safe to include.

*** Summaries carousel (=frontend/app/components/summaries/list.js=)
- Route loads all summaries via =store.findAll('summary')=
- Controller passes =store.peekAll('summary')= (all user summaries, not just resume-linked)
- =_navigate()= now PATCHes the summary with =active=true= + resume relationship
  (was incorrectly pushing into resume's hasMany and saving the resume)
- Position indicator added (e.g. "3 / 12")

** Key pattern: JSON:API relationship =data= linkage
When a serializer declares to-many =relationships=, =BaseSerializer.to_resource= only emits
=links= (no =data=). This is correct for relationships that are paginated or expensive.
For relationships that should resolve from =included= sideloads, the serializer's
=to_resource= override must add =data= arrays:

#+BEGIN_SRC python
# In the serializer's to_resource():
for rel_name, rel_type in rel_defs:
    _, items = self.get_related(obj, rel_name)
    linkage = [{"type": rel_type, "id": str(item.id)} for item in items]
    res.setdefault("relationships", {})[rel_name] = {
        "data": linkage,
        "links": existing_links,
    }
#+END_SRC

Without =data=, Ember Data has no way to match =included= records to their parent —
even if the records are in the payload.

** Key pattern: async hasMany proxy resolution
Even with proper sideloading, Ember Data's =async: true= hasMany returns a =PromiseManyArray=.
Accessing =.length= before resolution returns 0. Routes must =await= relationships:

#+BEGIN_SRC javascript
const resume = await this.store.findRecord('resume', resume_id);
await Promise.all([resume.skills, resume.summaries, resume.projects]);
// Now .length and .objectAt() work synchronously in templates
#+END_SRC

This await resolves instantly from the store (no extra fetch) when data is sideloaded.

* Full-height page layout (caddy chat)
:PROPERTIES:
:CREATED:  [2026-04-15]
:END:

** Problem
The =/caddy= full-page chat didn't fill the viewport. Input sat in the middle of the page
instead of at the bottom. The empty space below was unusable.

** Root cause: =overflow-y: auto= breaks flex height constraint
=.course-main= has =overflow-y: auto= by default (so normal pages scroll). When we add
=display: flex; flex-direction: column= for the caddy-fullpage override, =overflow: auto=
tells the browser the flex container can grow and scroll — so flex children don't get
constrained to the available height. They render at their natural size and the container
scrolls. Since the chat panel is smaller than the viewport, it just sits in the middle.

** Fix
=.course-main:has(.caddy-fullpage)= now sets =overflow: hidden=. This forces the flex
children to fit within the grid cell's actual height. The chat panel's own messages div
has =overflow-y: auto= so messages still scroll internally.

** Height propagation chain (all must be flex/grid with constrained overflow)
#+BEGIN_SRC
.course          → height: 100vh; display: grid; overflow: hidden
  .course-main   → grid-area: main; overflow: hidden (was auto — that broke it)
    :has(.caddy-fullpage) → display: flex; flex-direction: column
      #subnav-portal → auto height (breadcrumbs bar)
      .fairway       → flex: 1; min-height: 0; display: flex; flex-direction: column
        .caddy-fullpage → flex: 1; min-height: 0; display: flex; flex-direction: column
          Chat::Panel   → flex flex-col h-full
            header      → auto
            #chat-messages → flex-1 overflow-y-auto (this scrolls)
            input area  → auto (pinned to bottom)
#+END_SRC

** Safety check
- Flash messages use =position: fixed= — not affected by =overflow: hidden= on =course-main=.
- Subnav portal is small, takes =auto= height in the flex column. Not clipped.
- Caddy template passes no =@flashMessages= to =<RouteLayout>=, so no =<BarCart>= in flow.
- This override only fires when =.caddy-fullpage= is present (=:has()= selector). All other
  pages keep =overflow-y: auto= and scroll normally.

** Lesson for agents
If a flex column child won't fill available height, check the parent's =overflow= property.
=overflow: auto= or =overflow: scroll= on a flex container lets children overflow instead of
being constrained. Use =overflow: hidden= when you need children to fit.

* Job Queue Integration Plan
:PROPERTIES:
:CREATED:  [2026-04-15]
:END:
[[file:todo.org::*plan integration of a job job queue][↑ todo.org]]
Full plan: =~/.claude/plans/sequential-scribbling-wall.md=

** Recommendation: django-q2 with PostgreSQL broker
No new infrastructure. Uses existing PostgreSQL as broker. One pip dependency, one extra
Docker container running =manage.py qcluster=.

** Current daemon thread inventory (7 spawn points)
| Location | Task | Line |
|----------+------+------|
| =views.py= | Score generation | ~3670 |
| =views.py= | Cover letter generation | ~5095 |
| =views.py= | Answer generation | ~6038 |
| =scraper.py= | Legacy browser dispatch | ~122 |
| =scraper.py= | MCP browser dispatch | ~171 |
| =scraper.py= | A2A browser dispatch | ~214 |
| =job_post_extractor.py= | parse_scrape | ~436 |

** Implementation: 8 steps
1. Add =django-q2= to =api/pyproject.toml=, =django_q= to INSTALLED_APPS
2. =Q_CLUSTER= config: 2 workers, 5min timeout, PostgreSQL ORM, 4s poll
3. New =api/job_hunting/lib/tasks.py= — 5 task functions
4. Replace =threading.Thread= with =async_task()= in views.py (3 sites)
5. Refactor =scraper.py= — expose sync dispatch, wrap in =async_task()=
6. Refactor =job_post_extractor.py= — replace daemon thread
7. Add =worker= service to docker-compose files (same image, different command)
8. =manage.py migrate= creates django-q2 tables

** Unchanged
Frontend poller, hold_poller, status field pattern, API 202 responses, chat SSE.

* ActivityPub Integration Plan
:PROPERTIES:
:CREATED:  [2026-04-15]
:END:
[[file:todo.org::*plan the integration of activitypub][↑ todo.org]]
Full plan: =~/.claude/plans/sequential-scribbling-wall.md=

** Concept: federated job post sharing between Career Caddy instances

** Data model additions
- =JobPost.is_public= (bool) — opt-in sharing flag
- =JobPost.ap_id= (url) — globally unique ActivityPub ID
- =FederatedInstance= model — known remote instances (domain, inbox, public_key)
- =FederatedJobPost= model — received remote posts (read-only)

** New endpoints
- =GET /.well-known/webfinger= — instance discovery
- =GET /api/v1/ap/actor= — instance Actor profile
- =POST /api/v1/ap/inbox= — receive federated activities
- =GET /api/v1/ap/outbox= — public job posts as ActivityPub collection

** Phased approach
1. WebFinger + Actor + Outbox (read-only, discoverable)
2. Inbox + HTTP Signatures (receive shared posts)
3. Frontend "Shared Jobs" feed + import
4. Instance admin (peers, blocking, activity log)

** Key constraints
- Company shared by name, not FK (already the case)
- No user-to-user federation — instances share, not individuals
- Federated posts are read-only (import to local copy for editing)

* Shared Answer Components — Refactor Notes
:PROPERTIES:
:CREATED: [2026-04-15]
:END:

** Components created
- =<Answers::Show>= — renders answer content with markdown, edit link, and yielded =<:actions>= block
- =<Answers::List>= — iterates answers with view/edit/delete buttons, accepts =@showRoute=, =@editRoute=, =@onDelete=

** Key pattern: =Answers::Form= route-aware navigation
=afterSave()= in =answers/form.js= now derives the target route from =this.router.currentRouteName=:
=current.replace(/\.answers\.(new|edit)$/, '.answers.show')=
This eliminates =@onSave= callbacks from all nested answer routes.

** Gotchas for future agents
1. +*=modelFor= returns objects in some parent routes.*+ RESOLVED [2026-04-15]. All question show routes now return single question. Child routes use =modelFor= directly (parent IS the question).
2. +*Top-level =answers/show.hbs= is completely different.*+ RESOLVED [2026-04-15]. Now uses =<Answers::Show>= with =<:actions>= for copy button. Renders markdown.
3. +*Top-level =answers/edit.hbs= has a custom form.*+ RESOLVED [2026-04-15]. Now uses =<Answers::Form>= with =<:actions>= for delete button.
4. *=@showQuestion= on =<Answers::Form>=.* Only used when the answer is created outside the
   question context (e.g. from a "new question + answer" flow). In nested routes under a question
   show page, omit it — the question is already visible above.
5. *The =<:actions>= named block* on =<Answers::Show>= is optional. Use it for extra buttons
   (copy to clipboard, "All Answers" link). If not provided, only the Edit link shows.
6. *The =<:actions>= named block* on =<Answers::Form>= is optional. Use it for extra buttons
   (delete). If not provided, only Favorite + Save show.

** Files changed
| File | Change |
|------+--------|
| =components/answers/show.hbs= | Created — shared answer display component |
| =components/answers/list.hbs= | Created — shared answer list component |
| =components/answers/form.js= | =afterSave()= derives route from =currentRouteName= |
| =templates/job-posts/show/questions/show/answers/show.hbs= | → =<Answers::Show>= |
| =templates/job-applications/show/questions/show/answers/show.hbs= | → =<Answers::Show>= |
| =templates/questions/show/answers/show.hbs= | → =<Answers::Show>= with =<:actions>= |
| =templates/companies/show/questions/show/answers/show.hbs= | → =<Answers::Show>= (new) |
| =templates/*/answers/new.hbs= | Removed =@onSave=, =@showQuestion=; =this.model= not =this.model.answer= |
| =templates/*/answers/edit.hbs= | Removed =@onSave= |
| =routes/*/answers/new.js= | Returns single answer record, gets question from =modelFor= |

* Shared Component Refactor — Round 2
:PROPERTIES:
:CREATED: [2026-04-15]
:END:

** What was done
Completed migration of top-level answer routes and nested question show routes to shared components. Fixed route model code smell (multi-resource returns).

*** Top-level answer routes migrated
- =answers/show.hbs= → =<Answers::Show>= with =<:actions>= for copy button. Now renders markdown.
- =answers/edit.hbs= → =<Answers::Form>= with =<:actions>= for delete button. Controller stripped to just =deleteAnswer=.

*** Route model code smell fixed
- =job-posts.show.questions.show= — was returning ={jobPost, question}=, now returns single question
- =job-applications.show.questions.show= — was returning ={jobApplication, question, answers}=, now returns single question
- Child =answers/new= routes updated: =parent.question= → =parent= (parent IS the question)
- Dead =onSave= controllers deleted (2 files)

*** Shared component updates
- =Answers::List=: =@showRoute= is now optional (wrapped in ={{#if}}=). job-applications omits View button.
- =Answers::Form=: Added =<:actions>= named block for context-specific buttons (e.g. delete). Removed unused ={{yield}}=.

** Challenges and patterns for future refactors

1. *Back links without =@model=.* When a child route is active, =<LinkTo>= to an ancestor route resolves parent dynamic segments from the current URL automatically. No =@model= needed. Verified in job-posts and job-applications contexts.

2. *=<Panel::Card>= inconsistency.* job-applications =questions/show.hbs= wrapped content in =<Panel::Card>=, while job-posts and companies did not. Removed for consistency. Decision: question show templates use bare =<div class="space-y-4">= wrapper.

3. *=showAnswersList= getter pattern.* Every question show controller needs this to hide the answer list when a child answer route is active (prevents duplicate display). The getter checks =router.currentRouteName= against the show and show.index variants. This is duplicated across all three controllers — candidate for a mixin if more resources adopt it.

4. *Dead =@onSave= callbacks.* After =Answers::Form= gained =afterSave()= route derivation, =@onSave= props and their controllers became dead code but were never cleaned up. Pattern: always grep for dead callbacks after migrating to shared components.

5. *=@showRoute= optionality.* job-applications answer list intentionally omits the View button (Edit + Delete only). Shared list components should always make route args optional with ={{#if}}= guards.

6. *Controller convergence.* After refactoring, all three question show controllers (companies, job-posts, job-applications) are structurally identical: =showAnswersList= getter + =deleteAnswer= action with =.then()/.catch()=. Strong candidate for extraction if a fourth context appears.

** Files changed
| File | Change |
|------+--------|
| =components/answers/list.hbs= | =@showRoute= optional |
| =components/answers/form.hbs= | Added =<:actions>= block, removed unused ={{yield}}= |
| =templates/answers/show.hbs= | → =<Answers::Show>= with copy button in =<:actions>= |
| =templates/answers/edit.hbs= | → =<Answers::Form>= with delete in =<:actions>= |
| =controllers/answers/edit.js= | Stripped to =deleteAnswer= only |
| =routes/job-posts/show/questions/show.js= | Returns single question (was ={jobPost, question}=) |
| =controllers/job-posts/show/questions/show.js= | =this.model= is question, =.then()/.catch()=, removed =onAnswerSave= |
| =templates/job-posts/show/questions/show.hbs= | =<Questions::Item>= + =<Answers::List>=, no =@model= on back link |
| =routes/job-posts/show/questions/show/answers/new.js= | =parent= not =parent.question= |
| =routes/job-applications/show/questions/show.js= | Returns single question (was ={jobApplication, question, answers}=) |
| =controllers/job-applications/show/questions/show.js= | Added =showAnswersList=, =.then()/.catch()=, removed =onAnswerSave= |
| =templates/job-applications/show/questions/show.hbs= | =<Questions::Item>= + =<Answers::List>=, removed =<Panel::Card>= |
| =routes/job-applications/show/questions/show/answers/new.js= | =parent= not =parent.question= |
| =controllers/job-posts/.../answers/new.js= | DELETED — dead =onSave= |
| =controllers/job-applications/.../answers/new.js= | DELETED — dead =onSave= |

* Shared Component Refactor — Full Audit
:PROPERTIES:
:CREATED: [2026-04-15]
:END:

The questions/answers refactor (Round 2) established the gold standard pattern. This audit identifies every remaining instance of the same code smells across all resources.

** Code smell 1: Route models returning multiple resources

9 routes return ={ x, y }= objects instead of a single resource. The parent should come from =modelFor=.

*** job-posts.show child routes (5 violations)

| Route file | Returns | Fix |
|------------+---------+-----|
| =routes/job-posts/show/questions/index.js= | ={ jobPost, questions }= | Return =questions= only |
| =routes/job-posts/show/questions/new.js= | ={ question, jobPost }= | Return =question= only |
| =routes/job-posts/show/scores.js= | ={ jobPost, scores }= | Return =scores= only |
| =routes/job-posts/show/summaries.js= | ={ jobPost, summaries }= | Return =summaries= only |
| =routes/job-posts/show/job-applications/index.js= | ={ jobPost, jobApplications }= | Return =jobApplications= only |

All can get =jobPost= via =this.modelFor('job-posts.show')=.

*** job-applications.show child routes (2 violations)

| Route file | Returns | Fix |
|------------+---------+-----|
| =routes/job-applications/show/questions/index.js= | ={ jobApplication, questions }= | Return =questions= only |
| =routes/job-applications/show/questions/new.js= | ={ question, jobApplication }= | Return =question= only |

All can get =jobApplication= via =this.modelFor('job-applications.show')=.

*** companies.show child routes (1 violation)

| Route file | Returns | Fix |
|------------+---------+-----|
| =routes/companies/show/questions/new.js= | ={ question, company }= | Return =question= only |

Can get =company= via =this.modelFor('companies.show')=.

*** questions.show child routes (1 violation)

| Route file | Returns | Fix |
|------------+---------+-----|
| =routes/questions/show/answers/index.js= | ={ question, answers }= | Return =answers= only |

Can get =question= via =this.modelFor('questions.show')=.

*** Cascade impact per route fix
Each fix requires updating:
1. The route model hook (remove parent fetch)
2. Every template reading =this.model.parentName= (change to =this.model= or remove)
3. Every controller reading =this.model.parentName= (change to =this.model= or remove)
4. Any child routes calling =modelFor= on the fixed route that destructure the old object

** Code smell 2: Inline lists duplicating shared components

11 templates have inline ={{#each}}= loops with View/Edit/Delete buttons where a shared list component already exists.

*** job-posts/show nested templates (5)

| Template | Iterates | Shared component exists | Notes |
|----------+----------+------------------------+-------|
| =templates/job-posts/show/scores.hbs= | =this.model.scores= | =components/scores/list.hbs= | Inline table |
| =templates/job-posts/show/cover-letters.hbs= | =this.model= | =components/cover-letters/list.hbs= | Inline table |
| =templates/job-posts/show/scrapes.hbs= | =this.model= | =components/scrapes/list.hbs= | Inline table |
| =templates/job-posts/show/summaries.hbs= | =this.model.summaries= | =components/summaries/list.hbs= | Inline table |
| =templates/job-posts/show/questions/index.hbs= | =this.model.questions= | =components/questions/list.hbs= | Complex — has inline answer form with AI toggle |

The questions/index is the hardest. The inline answer form with AI polling is deeply coupled to the table. Needs its own component or a strategy pattern on =Questions::List=.

*** companies/show nested templates (2)

| Template | Iterates | Shared component exists | Notes |
|----------+----------+------------------------+-------|
| =templates/companies/show/answers.hbs= | questions | =components/questions/list.hbs= | Inline table with answer counts |
| =templates/companies/show/scores.hbs= | scores | =components/scores/list.hbs= | Inline table |

*** questions/show nested templates (1)

| Template | Iterates | Shared component exists | Notes |
|----------+----------+------------------------+-------|
| =templates/questions/show/answers/index.hbs= | answers | =components/answers/list.hbs= | Inline divs — straightforward swap |

*** Top-level index pages (3)

| Template | Iterates | Shared component exists | Notes |
|----------+----------+------------------------+-------|
| =templates/answers/index.hbs= | answers | =components/answers/list.hbs= | Panel::Card + infinity scroll |
| =templates/questions/index.hbs= | questions | =components/questions/list.hbs= | Panel::Card + infinity scroll |
| =templates/summaries/index.hbs= | summaries | =components/summaries/list.hbs= | Panel::Card + infinity scroll |

Top-level index pages have =InfinityLoader= and search. The shared list component would need to be the inner loop only, with pagination/search staying in the route template.

** Code smell 3: Dead callbacks

2 controllers have dead =onSave= actions never referenced by any template.

| Controller | Dead action | Safe to delete file |
|------------+-------------+---------------------|
| =controllers/job-posts/show/questions/show/answers/edit.js= | =onSave= | Yes — whole file is dead |
| =controllers/job-applications/show/questions/show/answers/edit.js= | =onSave= | Yes — whole file is dead |

** Open question: =<Panel::Card>= future :DECISION_NEEDED:

=<Panel::Card>= is used heavily in:
- Resume builder components (=experiences=, =educations=, =certifications=, =descriptions=)
- Top-level index pages (=answers/index=, =questions/index=, etc.)
- Some nested show templates

Two options:
1. *Deprecate* — replace all uses with Tailwind utility classes directly. Requires a large refactor of resume builder components.
2. *Keep and standardize* — make =<Panel::Card>= a thin wrapper around a consistent set of Tailwind classes (e.g. =rounded-lg border border-gray-200 bg-white p-4=). All list components and templates use it consistently.

Decision deferred — needs exploration of resume builder impact first.

** Completed
CLOSED: [2026-04-15]

1. +questions/show/answers/index+ — DONE. Swapped to =<Answers::List>=, fixed route model, deleted 2 dead edit controllers.
2. +job-posts/show/scores+ — DONE. Fixed route model, swapped to =<Scores::List>= with =@showJobPost={{false}}=.
3. +job-posts/show/summaries+ — DONE. Fixed route model, swapped to =<Summaries::List>=. Carousel renamed to =<Summaries::Carousel>=.
4. +job-posts/show/cover-letters+ — DONE. Rewrote =<CoverLetters::List>= as table. Deleted dead =list.js=.
5. +job-posts/show/scrapes+ — DONE. Swapped to =<Scrapes::List>= with =@showCompany={{false}} @showJobPost={{false}}=.
6. +job-posts/show/job-applications/index+ — DONE. Fixed route model. Dead =jobPost= getter removed.
7. +job-posts/show/questions/index+ — DONE. Fixed route model. Dead =jobPostId= getter removed.
8. +companies/show/scores+ — DONE. Swapped to =<Scores::List>= with =@showResume={{false}}=.
9. +companies/show/questions/new+ — DONE. Fixed route model.
10. +job-applications/show/questions/index+ — DONE. Fixed route model.
11. +job-applications/show/questions/new+ — DONE. Fixed route model.
12. All controller =jobPost= getters using =currentRoute.parent.params= — DONE. Removed from scores, summaries, cover-letters, scrapes, questions/index.

13. +companies/show/answers+ — DONE. Rewrote =<Questions::List>= as conformed table. Deleted dead =list.js= with unused company filter.
14. +Top-level index pages+ — DONE. =answers/index=, =questions/index=, =summaries/index= all use shared list components with =InfinityLoader= wrapping.
15. +=Questions::InlineAnswer= extracted+ — DONE. Inline answer form (AI toggle, textarea, polling) moved from =job-posts/show/questions/index= controller+template into self-contained component. Controller 131 → 42 lines.

** Remaining :TODO:

*** =job-posts/show/questions/index= table is still inline
The questions table in =job-posts/show/questions/index.hbs= can't use =<Questions::List>= directly because each row conditionally expands a =<Questions::InlineAnswer>= form row below it. The table headers + row markup are duplicated from the shared component. Options:
1. Live with it — the template is clean enough now with the InlineAnswer extracted
2. Make =Questions::List= yield each row so the consumer can inject the InlineAnswer =<tr>= — adds complexity for one use case

Recommendation: leave as-is. The inline answer expansion is unique to this context.

*** =/summaries= redesign with HyperUI details-list
The top-level =/summaries= page currently uses =<Summaries::List>= (truncated table). Full summary text is preferred here. Redesign using the HyperUI details-list pattern (https://www.hyperui.dev/components/application/details-list/) to show each summary with full content, resume, active status, and job post metadata. See todo.org.

*** Exception: =parent.params= + =peekRecord= for action-based record creation
The refactor eliminated controller getters that used =currentRoute.parent.params= to derive
parent models. However, =job-posts/show/scores= has a =scoreResume= action that creates a
new score record needing a =jobPost= relationship. This is NOT a typical form — the score
page isn't a "new" form with an in-memory object. It's a button that POSTs to the API.

When there are zero scores, there's no model relationship to follow back to the parent.
The job post IS in the store (loaded by the parent route), so =peekRecord= is safe.
=parent.params= is the correct way to get the ID in this case.

Pattern: =this.router.currentRoute.parent.params.job_post_id= → =this.store.peekRecord('job-post', id)=.

This is the ONE exception to the "no =parent.params= in controllers" rule. It applies when:
1. An action (not a getter) needs to create a record with a parent relationship
2. The model is a flat array (scores), not a hash
3. The parent is guaranteed to be in the store from the parent route's =model()=

*** Redundant parent columns in child contexts
Shared list components may show a parent column that's redundant when used in a child route (e.g. =companies/:id/scores= shouldn't show a Company column). The =@showCompany={{false}}= / =@showJobPost={{false}}= pattern handles this. Needs an audit pass to ensure all child contexts hide their parent column.

*** =<Panel::Card>= future :DECISION_NEEDED:
=<Panel::Card>= is used heavily in resume builder components (=experiences=, =educations=, =certifications=, =descriptions=) and some show templates. Top-level index pages no longer use it (migrated to shared list components). Two options:
1. *Deprecate* — replace all uses with Tailwind utility classes directly. Requires a large refactor of resume builder components.
2. *Keep and standardize* — make =<Panel::Card>= a thin wrapper around a consistent set of Tailwind classes. All list components and templates use it consistently.
Decision deferred — needs exploration of resume builder impact first.

** Shared component API reference

| Component | Args | Notes |
|-----------+------+-------|
| =<Answers::List>= | =@answers @showRoute @editRoute @onDelete @onToggleFavorite @emptyMessage= | All optional except =@answers= |
| =<Answers::Show>= | =@answer @editRoute= | =<:actions>= named block |
| =<Answers::Form>= | =@answer @showQuestion @onSave= | =<:actions>= named block, AI toggle, polling |
| =<Questions::List>= | =@questions @showRoute @editRoute @answerRoute @answersRoute @onDelete @emptyMessage= | All optional except =@questions= |
| =<Questions::Item>= | =@question @askAI= | Read-only display |
| =<Questions::InlineAnswer>= | =@question @onCancel @onComplete= | Self-contained answer form with AI/polling |
| =<Scores::List>= | =@scores @isPending @showJobPost @showResume @showStatus @showCreated @onDelete @emptyMessage= | =@show*= default to true |
| =<Scrapes::List>= | =@scrapes @isPending @showCompany @showJobPost @onRetry @emptyMessage= | =@show*= default to true |
| =<CoverLetters::List>= | =@coverLetters @isPending @showJobPost @emptyMessage= | Favorite display built in |
| =<Summaries::List>= | =@summaries @isPending @emptyMessage= | Table with Resume, Active columns |
| =<Summaries::Carousel>= | =@summaries @resume= | Prev/next navigation, resume builder only |

** AI prompt assembly — injected prompts
:PROPERTIES:
:CREATED:  [2026-04-15]
:END:

*** How AnswerService works (the reference implementation)

The answer generation pipeline:

#+BEGIN_SRC
Frontend POST /api/v1/answers/ { ai_assist: true, injected_prompt: "..." }
  → AnswerViewSet.create (views.py ~5940)
    → extracts injected_prompt from request payload
    → builds career_markdown from /career-data/ via ApplicationPromptBuilder.build_from_career_data()
    → spawns async thread calling AnswerService.generate_answer()
      → load_context_for_question(): resolves user, application, job_post, company, resumes, cover_letters, Q&A history
      → ApplicationPromptBuilder.build(context, injected_prompt=..., career_markdown=...)
        → assembles prompt in this order:
           1. Instructions (preamble — system rules)
           2. ## Additional Instructions (injected_prompt — caller-supplied directives)
           3. ## Question to Answer (the actual question)
           4. ## Career Profile (career_markdown, if provided — replaces resumes)
           5. ## Job Details
           6. ## Company
           7. ## Resume(s) Summary
           8. ## Cover Letters
           9. ## Q&A History
      → write_prompt_to_file() (debug only — writes to api/runtime/prompts/)
      → _call_ai(prompt) → OpenAI chat.completions
      → saves Answer record
#+END_SRC

The prompt is structured so =head= on any logged prompt file shows: what rules apply, what's being asked, and what special instructions were given — before the bulk of supporting context.

*** Current state of all AI services

| Service | File | Prompt method | Uses ApplicationPromptBuilder? | Injected prompt? |
|---|---|---|---|---|
| Answers | =answer_service.py= | =ApplicationPromptBuilder.build()= | YES | YES |
| Summaries | =summary_service.py= | Jinja2 (=summary_service_prompt.j2=) | NO | YES |
| Cover Letters | =cover_letter_service.py= | Jinja2 (=cover_letter_prompt.j2=) | NO | YES |
| Scores | =job_scorer.py= | Jinja2 (=job_scorer_prompt.j2=) | NO | YES |

=build_from_career_data()= is called by views.py for answers, summaries, cover letters, and scores — but only to render the career data into markdown. The actual prompt assembly for summaries/cover-letters/scores uses Jinja2 templates, not =build()=.

*** Pattern for adding injected_prompt to other services

Each service that gains injected_prompt support should follow the AnswerService pattern:

1. Accept =injected_prompt= as an optional parameter on the generate method
2. Place it after the preamble/instructions but before the question and supporting context
3. Pass it through to =write_prompt_to_file()= so logged prompts show it at the top
4. Frontend sends it as an attribute in the JSON:API payload (same as answers)

For Jinja2-based services (summaries, cover letters, scores), the template gains an ={% if injected_prompt %}= block after the system instructions block.

*** Prompt file readability contract

=head= on any =api/runtime/prompts/*.md= file should answer: /what is being asked and how?/

The top of every prompt file shows:
1. System instructions (the rules)
2. Additional instructions (the injected prompt — user's specific directives)
3. The question or task
4. Supporting context follows below

* Refactor: views.py → views/ package
:PROPERTIES:
:CREATED:  [2026-04-15]
:END:
[[file:~/.claude/plans/vast-squishing-waffle.md][Full plan]]

=views.py= is 8080 lines with 24 viewsets. Split into a =views/= package with 17 files.

** Key decisions
- =views/__init__.py= re-exports everything — =urls.py= unchanged
- =DjangoUserViewSet= inherits =BaseViewSet= (deletes 5 duplicated methods)
- =PaginatedListMixin= replaces copy-paste pagination in 8+ viewsets
- =ResumeRelationshipMixin= replaces ~30-line block in 4 viewsets (Experience, Education, Certification, Description)
- =ResumeViewSet= stays ~1455 lines (405-line =_upsert= is a single cohesive unit)

** File mapping (17 files)
| File | Contents | ~Lines |
|------+----------+--------|
| =_constants.py= | OpenAPI schema objects | 110 |
| =base.py= | BaseViewSet | 520 |
| =mixins.py= | PaginatedListMixin, ResumeRelationshipMixin | 100 |
| =standalone.py= | function views (healthcheck, profile, etc) | 640 |
| =user.py= | DjangoUserViewSet | 620 |
| =resume.py= | ResumeViewSet | 1455 |
| =resume_components.py= | Experience, Education, Certification, Description | 640 |
| =qa.py= | Question, Answer | 520 |
| =job_post.py= | JobPostViewSet | 430 |
| =scrape.py= | Scrape + ScrapeProfile | 480 |
| =cover_letter.py= | CoverLetterViewSet | 445 |
| =application.py= | JobApplication, Status, JobApplicationStatus | 370 |
| =score.py= | ScoreViewSet | 260 |
| =company.py= | CompanyViewSet | 180 |
| =project.py= | Project, Summary | 340 |
| =admin.py= | ApiKey, AiUsage, Waitlist, Invitation | 380 |

** Migration: 11 incremental steps, each passes =make test-api=
See full plan for step-by-step.

* Refactor: serializers.py — extract duplication into BaseSerializer
:PROPERTIES:
:CREATED:  [2026-04-15]
:END:

=serializers.py= is 1325 lines with 26 serializer classes. Unlike views.py, the file size is manageable — the problem is 3 copy-paste patterns that should be BaseSerializer methods.

** Pattern 1: Data linkage for to-many relationships (4 serializers)
ResumeSerializer, JobPostSerializer, ExperienceSerializer, ProjectSerializer each copy-paste a ~15-line loop:
#+BEGIN_SRC python
rel_defs = [("summaries", "summary"), ...]
for rel_name, rel_type in rel_defs:
    _, items = self.get_related(obj, rel_name)
    linkage = [{"type": rel_type, "id": str(item.id)} for item in items]
    res.setdefault("relationships", {})[rel_name] = {"data": linkage, "links": ...}
#+END_SRC

*Fix:* Add =linked_relationships= class attribute (list of rel names) to BaseSerializer. =to_resource()= auto-adds =data= linkage for those relationships. Serializers just declare the list.

** Pattern 2: User relationship linkage in =to_resource()= (4 serializers)
CoverLetterSerializer, JobApplicationSerializer, ApiKeySerializer, QuestionSerializer each inject =user= or =created_by= relationship data with the same ~8-line block.

*Fix:* Add =user_fk= class attribute (e.g. ="user_id"= or ="created_by_id"=). BaseSerializer.to_resource() auto-injects user relationship data when set.

** Pattern 3: User FK resolution in =get_related()= (5 serializers)
ScoreSerializer, CoverLetterSerializer, JobApplicationSerializer, ApiKeySerializer, QuestionSerializer each override =get_related()= with identical "look up User by FK" code.

*Fix:* BaseSerializer.get_related() checks =user_fk= attribute and handles the user lookup automatically. Subclasses only override for genuinely custom logic.

** Implementation: 3 steps, each passes =make test-api=
1. Add =linked_relationships= to BaseSerializer, migrate 4 serializers
2. Add =user_fk= to BaseSerializer, migrate 4 =to_resource()= overrides + 5 =get_related()= overrides
3. Clean up — remove empty =to_resource()= / =get_related()= overrides that are now no-ops

** Stats after refactor
- 16 serializers currently override =to_resource()= → ~8 would still need overrides
- 9 serializers currently override =get_related()= → ~3 would still need overrides (DjangoUser, Resume, Company have genuinely custom logic)
- Estimated ~150 lines removed across the file

** Note: no file split needed
Unlike views.py, serializers.py at ~1175 lines post-refactor is fine as a single file. The classes are small and cohesive.

** DONE
CLOSED: [2026-04-16 api]
Implemented all 3 patterns. 1325 → 1135 lines (190 lines removed). 265 tests pass, ruff clean.
- =user_fk= class attribute: auto-injects user relationship in =to_resource()= and =get_related()=. Migrated 7 serializers.
- =linked_relationships= class attribute: auto-populates data linkage arrays for to-many relationships. Migrated 4 serializers.
- Cleanup: replaced ExperienceSerializer inline =_dp()= with enhanced =_parse_date()= (now falls back to dateparser).

* Standalone Career Caddy Automation Toolkit (caddy_automation)
[[file:todo.org::*PROJ caddy_automation][↑ todo.org]]
Codename: =caddy_automation=. Folder: =career_caddy_automation/=.

#+NOTE: [2026-04-16] Initial extraction complete. Email removed from =ai/= (email_server.py,
email_classifier_agent.py, gateway.py deleted; agent_factory stripped; job_email_to_caddy.py
now URL-only). A2A orchestrator, hold_poller, discover_sites, manual_login all in standalone project.

** Goal
Separate personal automation (email pipeline, browser scraping) from the Career Caddy product.
The =ai/= directory ships with Career Caddy. Personal automation lives in its own repo that
/consumes/ the Career Caddy API — the same way any user would.

This project also serves as the reference implementation for anyone who wants to build their
own automation on top of Career Caddy's API or MCP endpoint.

** What stays in =ai/= vs what moves out

*** Stays in =ai/= (ships with Career Caddy)
| File | Purpose |
|------+---------|
| =chat_server.py= | Frontend chat via SSE — internal Docker service |
| =public_server.py= | =mcp.careercaddy.online= — public MCP endpoint |
| =hold_poller.py= | Scrape worker — polls for =status=hold= scrapes |
| =career_caddy_server.py= | MCP wrapper for internal agents (stdio) |
| =lib/api_tools.py= | Shared API client — used by both internal and external |
| =lib/toolsets.py= | =CareerCaddyToolset= — pydantic-ai tool wrapper |
| =lib/models/= | Shared pydantic models (JobPostData, CompanyData, etc.) |

*** Moves to =career_caddy_automation/= (personal + reference)
| File | Purpose |
|------+---------|
| =email_server.py= | notmuch email search/tagging — only works for the owner |
| =email_classifier_agent.py= | LLM-based email classification loop |
| =job_email_to_caddy.py= | Full pipeline: email → scrape → post |
| =browser_server.py= | Camoufox browser automation |
| =gateway.py= | Aggregates email + caddy + browser servers |
| =job_extractor_agent.py= | HTML → structured =JobPostData= |
| =career_caddy_agent.py= | Agent that does CRUD with duplicate checking |

** Two connection modes

The standalone toolkit can talk to Career Caddy two ways:

*** 1. Direct API (simple, for the owner)
Set two env vars and go:
#+begin_example
CC_API_BASE_URL=https://api.careercaddy.online   # or http://localhost:8000
CC_API_TOKEN=jh_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
#+end_example
The toolkit makes HTTP requests directly to the JSON:API endpoints.
This is what the current =ai/= layer does internally.

*** 2. MCP SSE (the "right" way for external consumers)
Connect a pydantic-ai agent to the public MCP endpoint:
#+begin_src python
from pydantic_ai.mcp import MCPServerSse

server = MCPServerSse(
    url="https://mcp.careercaddy.online/mcp",
    headers={"Authorization": "Bearer jh_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"},
)
#+end_src
This proves that =mcp.careercaddy.online= works for external agents and is the
pattern we'd recommend to other Career Caddy users.

The public MCP server (=public_server.py=) exposes career-data tools only:
- Companies: =create_company=, =find_company_by_name=, =search_companies=
- Job Posts: =create_job_post_with_company_check=, =find_job_post_by_link=, =search_job_posts=
- Applications: =create_job_application=, =get_applications_for_job_post=, =update_job_application=
- Career Data: =get_career_data=, =get_resumes=, =get_questions=, =score_job_post=
- Scrapes: =create_scrape=, =get_scrapes=, =update_scrape=

No email or browser tools — those are personal and run locally.

** Proposed project structure

#+begin_example
career_caddy_automation/
├── pyproject.toml              # pydantic-ai, httpx, fastmcp, camoufox (optional)
├── .env.example                # CC_API_BASE_URL, CC_API_TOKEN, OPENAI_API_KEY
├── .envrc                      # direnv: source .env
├── secrets.yml.example         # browser login creds template (optional)
├── README.md
├── src/
│   ├── client/                 # Reusable Career Caddy API client
│   │   ├── api_client.py       # Extracted from ai/lib/api_tools.py
│   │   ├── models.py           # JobPostData, CompanyData, APIResponse
│   │   └── toolset.py          # CareerCaddyToolset for pydantic-ai agents
│   ├── agents/                 # Reusable agents
│   │   ├── job_extractor.py    # HTML → structured JobPostData (no API needed)
│   │   └── caddy_poster.py     # Agent that creates job posts with duplicate checking
│   └── pipelines/              # Concrete automations (the examples people emulate)
│       ├── url_to_caddy.py     # Simplest: scrape one URL → post to Career Caddy
│       └── email_to_caddy.py   # Full: email search → scrape → post (needs notmuch)
├── mcp_servers/                # Local MCP servers (optional, for gateway mode)
│   ├── email_server.py         # notmuch integration
│   └── browser_server.py       # Camoufox browser automation
└── scripts/
    ├── tag_emails.py           # Daemon: classify + tag emails hourly
    └── process_tagged.py       # Daemon: process tagged emails → Career Caddy
#+end_example

*** What each layer does
- =src/client/= :: The reusable core. Anyone who wants to talk to Career Caddy's API
  from Python imports this. No LLM dependency — just =httpx= and =pydantic=.
- =src/agents/= :: Pydantic-AI agents that use the client. These /do/ need an LLM
  (OpenAI, Anthropic, or Ollama). The =job_extractor= parses raw HTML into structured
  data. The =caddy_poster= pushes it to the API.
- =src/pipelines/= :: End-to-end workflows. =url_to_caddy.py= is the minimal example
  (scrape a URL, extract job data, post it). =email_to_caddy.py= is the full pipeline
  (requires notmuch + browser automation).
- =mcp_servers/= :: Optional. Only needed if running the local gateway (aggregates
  email + browser + caddy tools on one SSE endpoint). Most users won't need this.
- =scripts/= :: Daemon loops. Personal automation that runs on a schedule.

** Configuration: how =.env= works

*** For personal use
#+begin_example
# .env
CC_API_BASE_URL=https://api.careercaddy.online
CC_API_TOKEN=jh_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NOTMUCH_MAILDIR=~/.mail
#+end_example
Load via =direnv= (=.envrc= contains =dotenv=) or =source .env=.

*** For someone else
They fork the repo, create their own =.env= with their Career Caddy instance URL and API key.
If they don't use email, they skip =NOTMUCH_MAILDIR= and only use =url_to_caddy.py=.
If they don't have Camoufox, they can still post job data directly via the API client.

*** What =CC_API_TOKEN= is
A long-lived API key generated in Career Caddy's web UI (Settings → API Keys).
Format: =jh_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx=. Sent as =Authorization: Bearer jh_xxx=.
The public MCP server validates it by calling =/api/v1/me/= on the Career Caddy API.

** Dependencies

*** Required (the reusable core)
- =httpx= — HTTP client for Career Caddy API
- =pydantic= — data models

*** Required for agents
- =pydantic-ai= — agent framework
- =openai= or =anthropic= — LLM provider (or Ollama for local)

*** Optional (personal automation)
- =fastmcp= — only if running local MCP servers
- =camoufox= + =playwright= — browser automation (~700 MB for the Firefox binary)
- =beautifulsoup4= + =html2text= — HTML parsing
- System: =notmuch= CLI — email indexing (Linux/macOS only)

** MCP verification plan

This is the first time we verify that =mcp.careercaddy.online= works for an external agent.

*** Steps
1. Generate a fresh API key in Career Caddy web UI (Settings → API Keys)
2. Write a minimal test script in =career_caddy_automation/=:
   #+begin_src python
   from pydantic_ai import Agent
   from pydantic_ai.mcp import MCPServerSse

   server = MCPServerSse(
       url="https://mcp.careercaddy.online/mcp",
       headers={"Authorization": "Bearer jh_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"},
   )

   agent = Agent("openai:gpt-4o-mini", mcp_servers=[server])

   async def test():
       async with agent:
           result = await agent.run("List my job posts")
           print(result.output)
   #+end_src
3. Confirm: auth succeeds (token validates via =/api/v1/me/=)
4. Confirm: =get_job_posts= returns data
5. Confirm: =create_job_post_with_company_check= creates a job post
6. Confirm: =find_job_post_by_link= finds it back
7. Document any issues (CORS, timeouts, auth failures, missing tools)

*** What could go wrong
- CORS: the MCP SSE endpoint may need CORS headers for browser-based clients
  (not relevant for Python agents, but worth noting for future web clients)
- Token validation: =ApiKeyTokenVerifier= calls =/api/v1/me/= — needs the API
  to be reachable from wherever =public_server.py= runs
- Timeout: long-running tool calls (scrape creation) may hit SSE timeouts
- Missing tools: =public_server.py= intentionally excludes email/browser tools —
  confirm the tool list matches what the standalone toolkit expects

** What someone else would do with this repo

1. Clone =career_caddy_automation/=
2. =uv sync= (or =pip install -e .=)
3. Copy =.env.example= → =.env=, fill in their Career Caddy URL + API key + LLM key
4. Run the simplest example:
   #+begin_example
   uv run python src/pipelines/url_to_caddy.py https://example.com/job/posting
   #+end_example
5. If they want to build their own pipeline, they import =src/client/= and write
   their own =src/pipelines/my_pipeline.py=

** GitHub-ready checklist
- [ ] README with quickstart (clone, env, run)
- [ ] =.env.example= with all vars documented
- [ ] =secrets.yml.example= (for browser creds, optional)
- [ ] =.gitignore= (=.env=, =secrets.yml=, =__pycache__/=, =.camoufox/=)
- [ ] MIT license
- [ ] No hardcoded tokens or secrets
- [ ] Entry points in =pyproject.toml= (e.g. =caddy-url=, =caddy-email=, =caddy-tag=)

** Relationship to the monorepo =ai/= layer

The standalone project /extracts/ code from =ai/=, it doesn't /import/ from it.
After extraction:
- =ai/lib/api_tools.py= remains the source of truth for the API client
- If the API changes, update =ai/lib/api_tools.py= first, then sync to the standalone project
- The standalone project is a /consumer/ of Career Caddy, not a part of it
- Future option: publish =src/client/= as =career-caddy-client= on PyPI so both
  the monorepo and the standalone project depend on the same package

* Electron Desktop App — Feasibility Study
[[file:todo.org::*PROJ Electron desktop app feasibility][↑ todo.org]]
/Placeholder — detailed study to be written under this heading./

* Profile-Aware Browser Agent
:PROPERTIES:
:CREATED: [2026-04-16]
:END:
[[file:~/.claude/plans/whimsical-juggling-cherny.md][Full plan]]
[[file:todo.org::*Agent-derived CSS selectors][↑ todo.org]]

** Motivation
The browser agent (=scrape_page()=) navigated blind — no knowledge of a domain's blocked
indicators, auth state, or job data structure. The =ScrapeProfile.css_selectors= JSONField
existed but was never read before scraping or populated by the browser.

** CSS selectors schema
Three categories. Login data stays in =sites.yml= only — never touches the API.

#+BEGIN_SRC json
{
  "blocked": {"paywall": ".paywall-overlay", "captcha": ".captcha-container"},
  "authenticated": {"indicator": ".global-nav__me"},
  "job_data": {"title": "h1.job-title", "company_name": ".company-name a",
               "description": ".job-description", "location": ".job-location"}
}
#+END_SRC

- =blocked= + =authenticated=: browser agent checks these after page load for state detection
- =job_data=: API-side =_try_tier0_extraction()= uses these for zero-cost extraction
- =login=: NOT stored — =sites.yml= via =Credentials.load()= handles login automation

** Changes made

*** =ai/lib/api_tools.py=
Added =get_scrape_profile(hostname)= and =update_scrape_profile(id, **attrs)=.
Wraps existing =GET /api/v1/scrape-profiles/?filter[hostname]=...= and =PATCH=.

*** =ai/mcp_servers/browser_server.py=
- =_check_profile_selectors(page, css_selectors)= — runs =query_selector= for each selector
  in =authenticated=, =blocked=, =job_data=. Returns results dict.
- =_discover_job_selectors(page)= — probes common job page patterns (=h1=, =.job-title=,
  =[data-testid*="title"]=, etc.) when no profile selectors exist. Returns ={field: selector}=.
- =scrape_page(url, profile=None)= — accepts optional profile dict, uses selectors for
  detection (blocked → error, authenticated → skip login wall), discovers new selectors,
  returns =selector_results= and =discovered_selectors= in response JSON.

*** =ai/scripts/hold_poller.py=
- Fetches scrape profile by hostname before scraping
- Passes profile to =scrape_page()=
- =_update_profile_from_results()= writes discovered selectors back to profile
- Logs warnings when existing selectors stop matching (site redesign detection)

*** =api/job_hunting/lib/parsers/job_post_extractor.py=
- =_try_tier0_extraction()= reads from =css_selectors.get("job_data", {})= (nested schema)
- =ProfileHints= model now includes =css_selectors= dict field
- =_generate_profile_hints()= asks LLM to propose CSS selectors alongside text hints
- Proposed selectors saved under =css_selectors["job_data"]= when none exist

** Other changes this session
- =updated_at= surfaced in scrape-profiles index table and show page
- =updating_profile= scrape status added (between extraction and completion)
- Chat server =create_scrape= now uses =status="hold"= (poller picks up)

** Security invariants
- Login selectors (=sites.yml=) never touch the API or scrape profile DB
- No credential data flows through =css_selectors=
- =api_tools.py= has no access to =secrets.yml= or browser automation code

* Scratch / Working Notes
(use this section for ephemeral notes during development)
* personal notes
